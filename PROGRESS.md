# PROGRESS

Dated one-line notes from the autonomous dev loop. Newest at the bottom.

- 2026-08-13 — M0: FastAPI backend skeleton (`backend/app/`) with `/` + `/health` (reports version + `llm_enabled`), CORS from env, settings via pydantic-settings, `requirements.txt`, `.env.example`, and pytest smoke tests (3 passing). Next: Next.js frontend skeleton calling `/health`.
- 2026-08-13 — M0: Shared synthetic city dataset. Deterministic stdlib generator `data/generate_city.py` (seeded) builds fictional 9×9 "Meridia" grid → `data/city/` (`zones.geojson` w/ pop/jobs/land-use, `roads.geojson` w/ capacity+cordon flags, `od_pairs.json` destination-constrained gravity commute flows ≈ jobs, `cbd_polygon.geojson` priced cordon, `manifest.json` provenance=Synthetic). Totals: 81 zones (9 CBD), 144 links (12 cordon-crossing), 2786 OD pairs, 253k pop / 154k jobs / 144.7k daily trips (54k into CBD). Backend cached loader `app/dataset.py` + 8 integrity tests (11 pytest total passing). Docs: `data/README.md`. Next: dev runner script + README run instructions.
- 2026-08-13 — M0: Next.js 14 (App Router) + TS frontend skeleton (`frontend/`). Landing page with SPEC-§34 tagging blurb + client `HealthStatus` component that fetches backend `/health` via `NEXT_PUBLIC_API_BASE_URL`; typed `lib/api.ts`, dark theme CSS, `.eslintrc`, `.env.local.example`, README. Verified: `npm run typecheck` + `next build` clean, and live smoke test (backend on :8010 `/health` ok, CORS preflight from :3000 → 200, prod frontend serves landing markers). Next: shared synthetic city grid dataset in `data/`.
- 2026-08-13 — M0 complete: Dev runner `scripts/dev.sh` boots backend (uvicorn) + frontend (Next.js) with one command — first-run venv creation, dep install, and dataset generation if missing; `setup`/`backend`/`frontend` subcommands; `BACKEND_PORT`/`FRONTEND_PORT`/`SKIP_INSTALL` overrides; children launched via `setsid` in own process groups with a trap that tears down the whole tree (uvicorn reloader + npm/next-server) on exit. Verified: combined run → `/health` 200 + frontend serves landing; SIGTERM cleanup leaves ports clear/no orphans; 11 pytest pass. README rewritten with verified run + `curl /health` verify steps. M0 scaffold done. Next: M1 `POST /policy/compile` (NL → Policy DSL).
- 2026-08-13 — M1: Policy compiler `POST /policy/compile` (NL → Policy DSL, SPEC §3). New `app/policy/` package: pydantic `PolicyDSL` (intervention/exemptions/revenue_allocation/stated_objectives/constraints), deterministic rule-based parser (`rules.py`, regex/keyword extraction of amount+currency, active hours, ISO date, intervention type, zone→cbd_polygon, exemptions, revenue split, objectives, equity constraint), optional Anthropic LLM path (`llm.py`, structuring-only, strict schema validation) that falls back to rules on any failure/absent key. Every field surfaced as reviewable `Assumption` (source stated/inferred/default + 0..1 confidence) per §3 "never bury assumptions"; response tagged provenance=Generated (§34). Config gains llm_provider/model/base_url/timeout; `.env.example` updated. 6 new tests (17 pytest total passing). Next: frontend policy input box + editable assumptions panel.
- 2026-08-13 — M1: Frontend policy input box + editable extracted-assumptions panel (SPEC §3). New client component `frontend/app/PolicyCompiler.tsx`: plain-language textarea (prefilled with the ROADMAP demo policy + "Load demo policy" button), `Compile policy` action calling new `compilePolicy()` in `lib/api.ts` → `POST /policy/compile`. Renders provenance/method/warnings tags (Generated + LLM/rule-based, §34) and every returned assumption as an editable control chosen by value type (bool→switch, number, list→comma-separated, text) via dotted-path get/set helpers in `lib/dsl.ts`; each row shows source badge (stated/inferred/default), confidence %, and rationale. Edits mutate the in-state DSL (shown live in a collapsible JSON view) that downstream simulation will consume. Verified: `tsc --noEmit` + `next lint` clean, `next build` OK, live smoke test against backend on :8021 returns real DSL+assumptions. Follow-up: rules parser doesn't recognise "credits" as a currency so `intervention.amount` stays null (surfaces as a default assumption needing human input) — fine for now; nested object assumptions (active_hours) render read-only. Next: M2 synthetic population (≥5k micro-agents).
- 2026-08-13 — M2: Synthetic commuter population (SPEC §6, ≥5k numerical micro-agents). New deterministic stdlib generator `data/generate_population.py` samples ~7985 agents from the OD trip table (weighted by `daily_person_trips`, so home/work geography reproduces commute flows) → `data/city/population.json` (provenance=Synthetic). Each agent: age, household_size, income + percentile-banded `income_band`, occupation, home/work zone, `commutes_into_cbd`, commute_distance_km, `car_access` (rises w/ income, lower for CBD homes), `public_transit_access` (by home category), modelled `baseline_commute_minutes` (overhead + dist/effective-speed + jitter; tagged as assumption), risk_aversion, price_sensitivity (falls w/ income), policy_salience (higher into priced district). File self-describes provenance/assumptions/summary; behavioural responses computed later by the engine, never LLM (§34). Backend `dataset.load_population()`/`population_agents()` + 6 integrity tests (23 pytest total passing). `scripts/dev.sh` auto-generates it if missing; `data/README.md` documents it. Verified: deterministic (identical md5 across two runs), all home/work zones referential-valid, bands cover population. Next: M2 baseline (World A) metrics — traffic, mode share, emissions proxy, transit demand.
- 2026-08-13 — M2: Baseline (World A) metrics — traffic, mode share, emissions proxy, transit demand (SPEC §5/§8/§34). New `app/baseline/` package: transparent input assumptions in `params.py` (`BaselineParams` — speeds, access overheads, per-km/fare costs, generalized-cost money→minutes weight, CO₂ factor 0.192 kg/veh-km, 2 trips/commuter/day, 250 workdays), a deterministic agent-based mode-choice model `model.py` (`choose_mode` = argmin generalized cost over feasible walk/car/transit for each commuter; time = overhead + dist/speed, money scaled by agent `price_sensitivity`; fixed tie-break so results are reproducible) and `compute_baseline()` aggregating the whole 7985-agent population into mode share (car 57.2% / transit 14.8% / walk 28.0%), traffic (car commuters, 9134 daily vehicle-trips, 14.8k veh-km/day, CBD cordon load), emissions proxy (2.84 tCO₂/day ≈ 709 t/yr = veh-km × factor) and transit demand (2370 boardings/day, peak CBD-bound). Every headline number emitted as a provenance-tagged `Metric` (all Simulated — structural model, no LLM) with method + named assumptions for the Evidence Drawer; params echoed for audit. 9 new tests (32 pytest total passing): mode-share partitions population, deterministic, no-car→never drives, short-trip→walk, tags=Simulated, traffic/emissions consistency. App still boots (baseline not yet wired to a route). Next: `GET /baseline` returns baseline metric time series.
- 2026-08-13 — M2 complete: `GET /baseline` returns baseline snapshot + metric time series (ROADMAP M2, SPEC §9/§34). New `app/baseline/timeseries.py` projects each provenance-tagged headline metric across the Time Machine checkpoints (T0, 1m, 3m, 5m, 1y, 2y, 5y, 10y): volume metrics (veh-km, CO₂, transit trips, cordon load) drift by a single transparent exogenous `demand_growth_per_year` (1.5%/yr compounded), mode-share **percentages** stay flat (no behaviour change without a policy), and every point carries a low/high confidence band whose relative half-width widens monotonically with the horizon (base 3% + 2%/yr, capped 30%) per SPEC §9. `BaselineTrend` assumptions echoed for the Evidence Drawer; all points tagged Simulated (deterministic transform of the structural snapshot, no LLM). New router `app/routers/baseline.py` (`GET /baseline` → `{world:A, snapshot, timeseries}`), wired into `main.py`. 6 new tests (38 pytest total passing): series covers all snapshot metrics, T0==snapshot, band widens monotonically & wider at 10y than T0, shares flat/volumes grow, zero-growth trend flat, endpoint 200 w/ Simulated tags + trend exposed. Verified via TestClient smoke (CO₂ 2.835→3.290 t/day over 10y, band ±3%→±23%). M2 done. Next: M3 mode-choice + traffic model applying the congestion charge/pedestrianisation (World B).
- 2026-08-13 — M3: World-B mode-choice + traffic model applying the congestion charge / pedestrianisation (SPEC §7.5/§7.7/§34). New `app/simulation/` package: `levers.derive_levers(PolicyDSL)` maps the compiled policy to explicit, auditable numeric levers — a per-one-way **cordon charge** (intervention.amount amortised over daily trips, for road_pricing/parking_levy/LEZ), **pedestrianisation** (cars banned from the CBD → CBD-bound commuters lose the car option, with a park-and-walk fallback for the 123 car-only/no-transit commuters so none are stranded), and **transit reinvestment** (revenue_allocation.public_transport buys a bounded fare cut ≤30% + speed uplift ≤15%, only when revenue is actually allocated). Exemptions parsed from DSL text → residents (home_zone∈CBD) / low-income (income_band) / disabled (visible only, no synthetic attribute). `model.choose_mode_policy` reuses the baseline generalized-cost machinery so World A/B are strictly comparable; `compute_world_b(policy)` re-aggregates the same 4 metric families into a provenance-tagged `WorldBMetrics` (all Simulated, no LLM) plus cordon accounting (priced_car_commuters/daily_priced_entries for the §10 revenue work) and per-lever `BehaviouralRule` audit records (SPEC §7.5: parameter/value/plausible_range/sensitivity/source). 13 new tests (50 pytest total passing). Smoke (demo cordon @12.5, residents exempt, 100% reinvest): car share 57.2%→39.9%, transit 14.8%→28.8%, CBD vehicle trips 3360→600/day, CO₂ 2.8→1.8 t/day. Not yet routed (that's the /simulate item). Follow-ups: no congestion-relief speed feedback yet (CBD car speed held constant); park-and-walk agents bucketed as walk. Next: M3 timeline checkpoints T0..10y with widening bands.
