# ROADMAP — ENGINE track (backend / simulation)

**This track owns ONLY:** `backend/**`, `data/**`, `scripts/**`, and this file + `PROGRESS_engine.md`.
**Never edit** `frontend/**`, `ROADMAP_UI.md`, `PROGRESS_ui.md`, `README.md`, `ROADMAP.md`, `SPEC.md`, `AGENT_LOOP.md`.
This keeps parallel tracks from colliding on git. Lock file: `.lock-engine`.

Work top-to-bottom. Each run, do as many items as you can finish cleanly (commit+push after
**each** item). Keep the app runnable and tests green at every commit. Respect SPEC §34
(LLMs never generate core numeric effects; tag metrics Observed/Estimated/Simulated/Generated;
label generated media SIMULATED; uncertainty widens with horizon).

## M3 — Policy simulation (World B) + time machine
- [x] `backend/app/simulation/` — apply the compiled Policy DSL to the mode-choice + traffic model: congestion charge raises car generalized cost inside the cordon; pedestrianisation removes car access on cordon links; revenue → bus frequency → transit access/speed improvement. Deterministic, no LLM. (SPEC §7.5/§7.7) [DONE in commit 197f244 — module is `backend/app/simulation/`, reuse it]
- [x] Timeline checkpoints (T0,1m,3m,5m,1y,2y,5y,10y): staged adaptation (behaviour substitution short-run, transit capacity ramp funded by revenue mid-run) with confidence bands that widen with horizon (SPEC §9/§24) [DONE — `backend/app/simulation/timeline.py`: interpolates 3 structural anchors (World A, reinvestment-off B, full B) via a fast behaviour ramp + lagged transit ramp; monotone widening band]
- [x] `POST /simulate` — body = Policy DSL (+ optional shocks/seed); returns World A, World B, Δ(B−A) per metric across checkpoints, tagged Simulated [DONE — `backend/app/routers/simulate.py` + `simulation/compare.py` (delta) + `simulation/shocks.py` (fuel/fare/demand shocks applied to both worlds)]
- [x] Event ledger (SPEC §10): structured events (e.g. transit capacity exceeded, cordon load drop) with cause/affected/confidence/downstream — this is the shared truth other engines read [DONE — `backend/app/simulation/events.py` derives mode_shift / cordon_load / transit_capacity / emissions / transit_reinvestment events from Δ(B−A), each with cause/affected_agents/confidence/downstream/evidence; exposed as `event_ledger` on `POST /simulate`]

## M5 — Parliament + amendment loop (backend)
- [x] `backend/app/parliament/` agents: Government, Opposition, Equity, Economist, Devil's Advocate. Each produces evidence-grounded arguments that cite `/simulate` metrics + event-ledger entries (LLM for prose only, with a deterministic template fallback when no key). `POST /parliament/debate` [DONE — `backend/app/parliament/` (personas deterministically select evidence + stance; `llm.py` polishes prose with template fallback); `POST /parliament/debate` returns 5 grounded arguments + tally + summary]
- [x] Amendment model + `POST /simulate` support for amended DSL (e.g. exempt bottom-30% income): recompute and return Δ vs original policy [DONE — `backend/app/simulation/amendment.py` (`Amendment` = structured DSL mutation) + `POST /simulate/amend` returns each policy's Δ-vs-baseline plus Δ(amended−original); `build_delta` generalised to compare two World-B runs]
- [x] Devil's Advocate → ranked Failure Mode Register (risk/mechanism/severity/probability/evidence/mitigation), SPEC §12 [DONE — `backend/app/parliament/failure_modes.py` + `POST /parliament/failure-modes`: adaptation-gap / regressive-backlash / revenue-erosion / assumption-fragility modes ranked by severity×probability; Estimated scores over Simulated evidence]

## M6 — Public reaction + simulated media (backend)
- [x] Cohort opinion model (income decile × geography × transport mode): material impact + fairness + prior → support distribution (Strong support…Strong oppose), deterministic core (SPEC §13). Expose via `/simulate` or `POST /public` [DONE — `backend/app/opinion/` + `POST /public`: per-agent material impact (own gc Δ) + fairness (regressivity/exemption/reinvestment/coercion) + income-band prior → 6-bucket distribution per cohort (band×geography×mode) and overall; deterministic, Simulated]
- [x] Simulated media generator `POST /media`: reads ONLY event ledger + outcome metrics + opinion state; emits archetype headlines (public-service/business/local/tabloid/etc.), every artifact tagged `SIMULATED — not a real outlet`, no real bylines (SPEC §15) [DONE — `backend/app/media/` + `POST /media`: 6 archetype lenses × 2 horizons (Month 5 / Year 2), each headline built from ledger events + Δ metrics + opinion, SIMULATED banner, fictional outlet labels, cited_refs]

## M7 — Evidence, uncertainty, credibility (backend)
- [x] Evidence/provenance trace endpoint: given a metric id, return the causal trace input-data→transform→model→assumptions→result + confidence (SPEC §26) [DONE — `backend/app/evidence/` + `POST /evidence`: re-runs the deterministic A/B/Δ sim, locates the metric's Δ trajectory, and assembles the input-data→transform(levers)→mode-choice model→assumptions→result ladder + an ASCII trace, the behavioural rules/equations touching the metric, named assumptions, illustrative real-world analogues (London/Stockholm/Singapore/Milan, Observed, flagged non-source), citations, and a horizon-widening confidence. Numbers copied from the sim only — no LLM (SPEC §34). 404 lists valid keys. 7 tests; 112 green]
- [x] Uncertainty engine: Monte Carlo / parameter sweep over key elasticities → median + 50/80/95 intervals + most-influential-assumption ranking (SPEC §24) [DONE — `backend/app/uncertainty/` + `POST /uncertainty`: triangular Monte-Carlo sweep over 8 documented uncertain assumptions (mode-switch elasticity, transit service/fare response, congestion feedback, transit speed, car cost, fare, CO₂ factor), each sample re-running the deterministic A/B/Δ model → median + 50/80/95% intervals **per checkpoint** (a fan of plausible futures widening with horizon); one-at-a-time sensitivity ranking of most-influential assumptions; low/central/high behavioural-regime ensemble → model disagreement. Seeded/reproducible, samples clamped 20–500, no LLM (SPEC §34). 8 tests; 120 green]
- [x] Counterfactual comparison endpoint: World A vs B vs amended C in one payload (SPEC §21) [DONE — `backend/app/simulation/counterfactual.py` + `POST /compare`: World A (baseline) vs World B (intervention) vs one world per supplied amendment (C, D…), each with snapshot + trajectory + Δ-vs-baseline + Δ-vs-intervention, plus a headline table (baseline always present + every world value + Δ per metric at one horizon). Amendments reuse `apply_amendment`; every number from the deterministic model, no LLM (SPEC §21/§34). 5 tests; 125 green]

## Stretch (only if all above done)
- [x] Policy optimiser stub: given objective+constraints, grid-search a few candidate interventions → Pareto set (SPEC §22) [DONE — `backend/app/optimiser/` + `POST /optimise`: grid-searches 25 candidate policies (congestion charge × amount × revenue split × low-income exemption, parking levy, pedestrianisation), simulates each with the deterministic World-B model + cohort opinion model, computes emissions/traffic/transit deltas + size-weighted avg-commute & low-income-burden % (real, from opinion cohorts' generalized-cost impact) + net support, applies the supplied objective/constraints (emissions target, max commute increase, max low-income burden, max budget), builds a 4-objective Pareto frontier and labels cheapest / most-equitable / largest-emissions-cut / best-balanced picks. Outcomes Simulated; only budget cost is a documented Estimated proxy; unsatisfiable constraints flagged but a frontier still returned. No LLM (SPEC §22/§34). 8 tests; 133 green]
- [x] Backtesting harness scaffold (SPEC §25) [DONE — `backend/app/backtest/` + `GET /backtest/example` + `POST /backtest`: historical replay of a `HistoricalCase` (policy + known outcomes) through the deterministic model using pre-implementation state only, scored against actuals — forecast error (MAE/RMSE/MAPE), direction accuracy (vs baseline), interval calibration (actuals inside the forecast band), and event-timing error (predicted vs actual event months, matched by type from the ledger). Built-in synthetic Meridia-2018 cordon benchmark case (actuals clearly labelled Simulated/not-real); geographic + full distributional accuracy flagged as unscored in the scaffold (no per-zone actuals) rather than silently skipped. Forecast Simulated, scores exact, no LLM (SPEC §25/§34). 7 tests; 140 green]

## Extended (SPEC coverage beyond original milestones)
- [x] SDG alignment layer (SPEC §23) [DONE — `backend/app/sdg/` + `POST /sdg`: maps the deterministic World-A/World-B sim + cohort-opinion burden model + the run's own audit artifacts onto UN SDG targets. Core SDG 11 (sustainable mode share 11.2, CBD private-vehicle trips 11.2/11.6, peak transit ridership 11.2) and SDG 16 (evidence-provenance completeness of decision metrics 16.6/16.10, count of structured cause→effect→confidence records 16.7 — governance-*process* proxies, not transport outcomes, measured from the run's provenance tags + event ledger); secondary SDG 13 (transport CO₂ 13.2) and SDG 10 (excess travel-cost burden on lowest-income vs average commuters 10.4, from the same generalized-cost model the optimiser uses). Each indicator carries SPEC §23's mandated shape — indicator/proxy, baseline, scenario, change, data source, confidence — with horizon-widening confidence. **No arbitrary composite "SDG score"** (SPEC §23): the report only counts improved/worsened/unchanged indicators. Transport indicators Simulated, proxy/opinion-derived ones Estimated, none Generated; deterministic, no LLM (SPEC §23/§34). 8 tests; 148 green]
- [x] Multi-agent institutional layer (SPEC §18) [DONE — `backend/app/institutions/` + `POST /institutions/review`: the institutional agents SPEC §18 asks for *beyond* parliament's five — **Climate**, **Implementation**, **Legal/Constitutional Research**, **Auditor** — each assessing the policy against a professional mandate (not a political stance). Reuses the parliament `simulate_brief`/`DebateBrief` so all four read the exact same deterministic evidence: Climate scores the commuter-CO₂ Δ + emissions event vs decarbonisation (clear ≥10% cut, else conditional) and flags rebound risk; Implementation detects the adaptation-gap (transit capacity exceeded before the funded uplift lands → concern with front-loading rec) and car-ban access management; Legal reasons from policy structure (legal base/vires + proportionality for any charge, indirect-discrimination concern when a flat charge has no low-income/resident exemption, access-rights duties for pedestrianisation); Auditor assesses the *evidence itself* — all Δ metrics tagged Simulated & LLM-free, bands widening to horizon, event-ledger causal trail — and clears on process even when the policy is weak. Structured `Finding`s (severity info/watch/risk/blocker) + citations to specific metrics/events, per-agent `Verdict` (clear/conditional/concern/block), an overall = worst-verdict rollup, tally and deterministic synthesis. Prose Generated, every cited number Simulated, no LLM in the numeric path (SPEC §18/§34). Parliament left untouched (imported, not edited). 5 tests; 177 green, app boots with 26 routes]
- [x] Ensemble forecasting (SPEC §8) [DONE — `backend/app/ensemble/` + `POST /ensemble`: estimates the flagship outcome (reduction in vehicle trips entering the central cordon) with three *methodologically-independent* estimators and pools them into a weighted ensemble whose band spans their disagreement (SPEC §8's honest-uncertainty point). (1) **Structural agent-based** (SPEC §7.5) — the deterministic World-B model's own Δ% (Simulated, weight 0.5, ±15% internal range); (2) **historical-analogue transfer** (SPEC §7.1) — a Michaelis–Menten saturating transfer calibrated on real flat-cordon schemes (asymptote ≈ −30%, half-sat per one-way charge) scaled by this policy's charge, with a 0.6–1.25× cross-scheme spread (Estimated, weight 0.3, illustrative anchors not this city's data, N/A to a pure car ban); (3) **reduced-form elasticity** (SPEC §7.2) — a low out-of-pocket price elasticity (≈−0.09, range −0.06…−0.13) of cordon car trips applied to the charge as a % of daily car money cost (Estimated, weight 0.2, N/A when no charge). Pools applicable methods by renormalised documented weights → central + a band = [min low, max high] across methods, a method-spread disagreement measure with low/moderate/high label + plain-language interpretation. All % clamped to physically-valid [−100,0]. Honestly surfaces that the ABM's ~−90% cordon collapse is far more extreme than real analogues (~−18%) → "high disagreement, treat magnitude as uncertain". Ensemble tagged Estimated (cross-method blend); deterministic, no LLM (SPEC §8/§34). 5 tests; 172 green, app boots with 25 routes]
- [x] Press conference simulation (SPEC §16) [DONE — `backend/app/press/` + `POST /press-conference`: stages the post-announcement press conference from the run's own numbers. A government spokesperson gives an opening statement (cordon-traffic Δ, commuter-CO₂ Δ, reinvestment) then faces five archetype journalists — public broadcaster (is it working?), business press (delivery/access cost pass-through), tabloid (populist "war on drivers", cites opposition %), environment correspondent (not ambitious enough on climate), and a local/opposition reporter (distributional fairness, names the worst-hit cohort by material impact) — each question grounded in a specific Δ metric / event-ledger entry / opinion figure, each with a spokesperson answer (stance defends/acknowledges/rebuts/commits) that cites the same figures and reflects the policy's real exemptions + reinvestment. Reuses the `/media` horizon-state reader; every number copied from the deterministic sim, an optional LLM polishes prose only (template fallback when no key), whole conference tagged Generated + SIMULATED banner, fictional outlets/reporters only (SPEC §16/§34). 5 tests; 167 green, app boots with 24 routes]
- [x] Model registry / transparency manifest (SPEC §33) [DONE — `backend/app/registry/` + `GET /registry`: a self-describing catalogue of every forecast layer (baseline ABM, World-B policy sim, Time Machine, Monte-Carlo uncertainty, cohort opinion, FJ diffusion, optimiser, parliament, media) with its SPEC sections, method, determinism class, output provenance tag, and — crucially — whether an LLM touches numbers (asserted False for every numeric layer, SPEC §34). Each numeric model publishes its documented assumptions **read live from the code** (`DEFAULT_PARAMS`, `DEFAULT_SIM_PARAMS`, `DEFAULT_ADAPTATION`, `OpinionParams`) so the manifest can't drift from what runs; plus data-source cards (synthetic population / assumption sets / policy DSL) and the SPEC §34 guardrail checklist (no-LLM-numbers, provenance tags, SIMULATED media, widening uncertainty, reproducibility) each with how it's enforced + a holds flag. Flat de-duplicated assumption index + summary counts. The registry is Observed about itself (transparency artifact, not a sim output); fully deterministic, no LLM. 5 tests; 162 green, app boots with 23 routes]
- [x] Economic spillover layer (SPEC §7.4) [DONE — `backend/app/economy/` + `POST /economy`: the transparent input-output / elasticity layer SPEC §7.4 asks for as the MVP economic engine. Reads the deterministic World-A/World-B mode-choice sim's **Simulated** physical outputs (cordon-charge revenue, Δ CBD car commuters, Σ Δ commuter travel-minutes from mode switches, freight-entry proxy) and translates them into five local-economy channels via documented coefficients (`backend/app/economy/params.py`): (1) **charge transfer** — commuter revenue withdrawn from household discretionary local spend (−R×MPC); (2) **revenue recycling** — the full collected cordon revenue (commuter Simulated + freight Estimated) re-spent locally at a fiscal multiplier, usually flipping the charge net-positive; (3) **CBD footfall** — a small destination-substitution loss from deterred car trips offset by a pedestrianisation retail-amenity uplift (ambiguous sign; commuter footfall conserved, shopper demand explicitly NOT modelled); (4) **business logistics** — freight/delivery charge passed through to CBD business (Estimated, low confidence, freight not agent-modelled); (5) **commuter travel cost** — the private mode-switch time cost monetised at a value of time = 1/`money_to_minutes` (consistent with the GC model). Rolls up per-sector exposure (retail/hospitality, transit-operations, construction, logistics, households) and a **net partial-equilibrium annual estimate with a wide band** — clearly labelled Estimated, NOT a GDP figure. Differentiates policies correctly (charge+reinvest net-positive, general-fund near-neutral, pedestrianisation net-negative). Honest `not_modelled` surface (congestion-relief time savings needs the spatial traffic-assignment layer; agglomeration/land-value; firm relocation; shopper demand; labour-market GE; fiscal-multiplier crowding-out). Physical drivers Simulated, monetary translation Estimated (SPEC §8); confidence widens with horizon; deterministic, no LLM (SPEC §7.4/§34). 8 tests; 185 green, app boots with 27 routes]
- [x] System dynamics / recursive feedback loop (SPEC §7.6 + §19) [DONE — `backend/app/dynamics/` + `POST /dynamics`: the stocks-and-flows engine SPEC §7.6 asks for, closing the recursive feedback loop §19 calls "central to the concept" and which the rest of the pipeline leaves open (World-B is a single adapted end-state; the Time Machine is a staged interpolation — neither lets opinion feed back into the *policy itself*). Integrates four coupled stocks month-by-month over the 10-yr horizon: **charge** (starts at the nominal amount, cut by an **endogenous amendment** when support stays negative), **transit demand** (relaxes toward the current charge's behavioural pull), **transit capacity** (a supply stock that only grows as cumulative reinvested revenue funds the capacity programme, with a build lag), and **support** (relaxes toward its structural target minus a sustained-crowding penalty). Instantiates the exact §19 cascade: charge → mode shift → revenue → capacity, and negative support → amendment → weaker charge → reduced revenue → slower capacity → renewed crowding. Every magnitude each stock chases is read from the deterministic ABM at the in-force charge (behavioural-only World B for demand, priced-commuter revenue, cohort-opinion net support) — memoised per distinct charge so it stays cheap — while the temporal coefficients coupling them live in `params.py` as documented Estimated assumptions. Emits coupled stock trajectories at the Time-Machine checkpoints, structured **feedback events** (capacity_exceeded / amendment / crowding_relieved, each with its causal chain), and — always — a **closed-loop vs open-loop contrast** (political-response ON vs OFF from the identical model) that concretely demonstrates why recursive feedback matters (the demo charge triggers 2 amendments, ending at £4.32 vs £12 with +0.23 support and −0.37 crowding). Reinvestment grows capacity; a general-fund split never does; a no-charge policy (pedestrianisation) has an inert political arm but still tracks crowding. Confidence widens with horizon; deterministic, no LLM (SPEC §7.6/§19/§34); registered in the §33 model registry (11 layers now). 8 tests; 193 green, app boots with 28 routes]
- [x] Microsimulation layer (SPEC §7.3) [DONE — `backend/app/microsim/` + `POST /microsim`: the person-level distributional engine SPEC §7.3 asks for by name — *Who gains? Who loses? By how much? Which decile? Which neighbourhood? Which household type?* — which no endpoint answered directly (the opinion model outputs a Likert *support* distribution, the optimiser a single low-income-burden %; neither is a £-and-minutes who-gains/who-loses microdata table). For every synthetic commuter it computes the change in **minimum generalized cost** between World A and World B under the *same* deterministic mode-choice model as `/simulate` — so a commuter who re-optimises is credited with the cost of their *new* best option, not a counterfactual they'd never take — plus the out-of-pocket charge each actually pays, then rolls the welfare change up across **income deciles** (ranked from the synthetic incomes), **household size**, **home neighbourhood** (central vs outer + most-affected zones) and **occupation**. Each group reports mean gc-change (minutes-equiv), a money-equivalent (Estimated, via the population value-of-time `money_to_minutes`), mean charge paid/day, mean annual-charge **burden as % of income**, and %worse-off / %better-off / %switched-mode. Headline: winners vs losers vs unaffected, charge payers, and a **regressivity ratio** (bottom-decile burden ÷ top-decile burden) with a plain-language verdict. On the demo £12 full-reinvest charge: 1438 winners (transit users gaining from the reinvested service) / 1679 losers / 4868 unaffected; the flat charge is **3.6× regressive** on out-of-pocket burden (bottom vs top decile) even though more *high*-income commuters are worse off overall (they drive more) — both facts surfaced honestly. A low-income exemption zeroes the bottom-decile burden (regressivity→0); a general-fund charge produces no transit winners. Welfare change Simulated, money-equivalent Estimated; fully deterministic, no LLM (SPEC §7.3/§34); registered in the §33 registry (13 layers now). Honest `not_modelled`: commuter travel welfare only (no wider household budget, labour-supply or in-kind reinvestment gains attributed back to individuals); single population value-of-time; synthetic-sample deciles, not real microdata. 8 tests; **210 green**, app boots with **30 routes**.]
- [x] Spatial traffic-assignment layer (SPEC §7.7) [DONE — `backend/app/spatial/` + `POST /spatial`: the explicit-geography layer SPEC §7.7 asks for, and the gap every other layer flagged as `not_modelled` ("needs §7.7", "spatial traffic assignment", "per-corridor detail"). Builds a directed graph from the real Meridia road grid (`roads.geojson`: 81 zones, 144 undirected links → 288 directed arcs, each with a per-direction `capacity_veh_per_hr`, free-flow speed and length) and runs a peak-hour **static traffic assignment** — Method-of-Successive-Averages over all-or-nothing shortest-path loadings with the standard **BPR** volume-delay function (t = t₀·(1+α(v/c)^β)) — to an approximate **user equilibrium** where drivers have re-routed until no faster path exists. Demand is generated by the *same* deterministic mode-choice model as `/simulate`: every synthetic commuter is re-evaluated (World A `choose_mode`, World B `choose_mode_policy`) and only those who still choose to drive load their home→work trip onto the network, so the spatial layer can never disagree with the ABM's mode split (SPEC §34). Sample car trips are expanded to city scale by a **representation factor read live from the OD table** (≈18.1 = 144.7k commute flows ÷ ~8k sampled agents) so peak-hour link volumes are comparable with the real-scale capacities; peak-hour concentration + car occupancy convert persons→vehicles (documented Estimated inputs). Read-out per world + Δ: congested link flows / v/c / speeds, **cordon inflow** (peak veh/hr entering the CBD), total **network vehicle-hours** & vehicle-km, gravity **job accessibility** (Aᵢ = Σⱼ jobsⱼ·exp(−decay·congested_timeᵢⱼ)) with biggest gaining/losing zones, and a per-zone **road-CO₂ dispersion proxy** (arc veh-km × CO₂ factor, attributed to endpoint zones, neighbour-smoothed) showing where central pollution falls and whether it displaces outward, plus notable arcs (cordon + biggest reroutes) and over-capacity bottleneck lists. The demo £12 charge cuts cordon inflow −89.6% (9768→1015 veh/hr), network vehicle-hours −41%, central CO₂ −80%, nudges accessibility +0.2% and lifts mean congested speed 47.4→49.3 km/h; a no-op policy leaves the network identical; pedestrianisation collapses cordon inflow. Fully deterministic, no LLM (SPEC §7.7/§34); registered in the §33 model registry (12 layers now). Honest `not_modelled`: single AM peak only (no time-of-day / freight / non-commute trips); static within-day equilibrium (no departure-time or day-to-day learning); pedestrianisation applied demand-side only (no physical road-closure rerouting on the graph); CO₂ is a smoothed dispersion *proxy* not an air-quality plume; transit not spatially assigned. 9 tests; **202 green**, app boots with **29 routes**.]
- [x] Social network / opinion diffusion (SPEC §14) [DONE — `backend/app/diffusion/` + `POST /diffusion`: builds an abstract social graph — 5 citizen-cohort nodes (one per income band, seeded from the deterministic cohort-opinion model) + journalists, government, opposition, business, influencers, community groups, institutions (documented opinion priors derived from who proposes the policy / who it costs / how the burden falls) — with a typed, row-stochastic influence matrix (social influence / media exposure / geography / workplace / political affinity / institutional edges). Runs a deterministic **Friedkin–Johnsen** diffusion (each node moves toward its neighbours' weighted-average opinion while staying partly anchored to its own conviction) over N information rounds → per-node opinion trajectories, issue salience (engagement) + polarisation per round, and the coalitions (support/oppose/contested) that form, plus the dominant narrative and citizen net-support drift. Optional narrative **information shocks** (round/node/delta, e.g. a scandal) durably shift the target's conviction so effects propagate. Opinions bounded [-1,1]; fully deterministic, no randomness, no LLM (SPEC §14/§34). Rounds are diffusion steps, explicitly NOT the physical Time-Machine horizon. 9 tests; 157 green, app boots with 22 routes]
- [x] Run reproducibility manifest — REPRODUCE RUN (SPEC §32) [DONE — `backend/app/reproduce/` + `POST /reproduce`: the per-run provenance envelope SPEC §32 requires ("Every run must store: dataset versions, model versions, parameters, random seed, prompts, policy DSL, assumptions, code version, timestamp … click REPRODUCE RUN and generate the same scenario"). Every other layer *asserts* determinism; nothing packaged the record that lets a user regenerate an identical run. Takes the same inputs as `/simulate` (policy DSL + optional shocks + seed) and returns a manifest whose **`run_id` is a SHA-256 content address** over the reproducing inputs — canonicalised policy DSL, shocks, seed, each **dataset pinned by the hash of its actual bytes** (`data/city/manifest.json` seed 42, `population.json` seed 42), the live de-duplicated **assumption/parameter index read straight from the §33 registry** (so it can't drift from what runs), `app_version` and **git `code_version`** (commit + `+dirty` flag, subprocess with a documented fallback when git is absent) — with the wall-clock **timestamp deliberately excluded** so identical inputs always yield the same `run_id` (that's what makes REPRODUCE RUN meaningful, not decorative). Proves reproducibility rather than claiming it: runs the deterministic A/B/Δ core **twice** and only sets `reproducible: true` when the two canonical `output_digest`s (rounded-float sha256 of World A/B + Δ) match. `run_id` is stable across calls, changes when the policy/shocks/seed/code/datasets change; seed is hashed into identity (SPEC §32) yet leaves the numbers unchanged (deterministic core). `prompts: []` asserted — **no LLM prompt enters the numeric path** (SPEC §34) — and every pinned model card carries `llm_touches_numbers: false`. Registry (§33) left untouched — §32 is a per-run record, §33 a static catalogue; kept separate. `models` pins all 13 forecast layers; manifest is Observed about the run (transparency artifact, not a forecast). Deterministic apart from the excluded timestamp; no LLM (SPEC §32/§34). 8 tests; **218 green**, app boots with **31 routes**.]
- [x] Stress-testing environment — external-shock scenarios (SPEC §20) [DONE — `backend/app/stress/` + `POST /stress-test` (+ `GET /stress-test/catalogue`): the stress-testing environment SPEC §20 asks for by name — *"policy performs well under baseline but fails under [recession][fuel shock]"* — which no endpoint delivered (the `Shocks` primitive existed but only as three raw multipliers threaded through `/simulate`; nothing packaged the named scenario toggles or the robustness read-out §20 specifies). Ships the exact SPEC §20 toggle set as a transparent, audited catalogue (`catalogue.py`): **recession, fuel-price spike, flood, heatwave, population growth, migration change, technology adoption, interest-rate shock**, each mapped to documented `Shocks` knobs (car cost-per-km / transit-fare / background-demand-growth) with a rationale — no hidden randomness (SPEC §20: "scenario assumptions, not secretly random events"). Re-runs the *same* deterministic A/B/Δ core as `/simulate` once per scenario (shock applied to **both** worlds so Δ(B−A) keeps isolating the policy, SPEC §21) and, at a chosen horizon, compares the policy's benefit on four headline metrics (cordon vehicle-trips, commuter CO₂, car mode-share, transit trips) under each shock to its benefit under the no-shock baseline → a per-metric verdict (**robust / strengthened / weakened / neutralised / reversed**) with the % of benefit retained, a per-scenario roll-up (**holds / degrades / fails**), and an overall robustness split (robust_to / degrades_under / fails_under). **Honest fidelity (SPEC §34):** each scenario declares `modelled` / `partial` / `proxy` and a plain caveat — fuel-price spike is represented directly; recession/population/migration/tech act through the aggregate-demand + car-cost channels the mode-choice core actually carries; flood/heatwave/interest-rate are flagged as proxies the static commuter model represents weakly (the interest-rate bite really lives in the §7.4 economy layer, the tech scenario UNDER-states CO₂ because tailpipe factor is held constant) rather than faking precision, and confidence widens at long horizons. Policy deltas Simulated, shock magnitudes Estimated; fully deterministic, no LLM (SPEC §20/§34); 404 lists valid keys. Registry (§33) left untouched — stress-test is a scenario harness over existing layers (like `/uncertainty` / `/compare`), not a new numeric model. 9 tests; **227 green**, app boots with **33 routes**.]
- [x] Historical Analogue / Causal Layer as a first-class endpoint (SPEC §7.1) [DONE — `backend/app/analogues/` + `POST /analogues` (+ `GET /analogues/cases`): the causal/empirical layer SPEC §7.1 asks for by name, previously present only as a thin saturating transfer function *inside* the ensemble (`app/ensemble/model.py`) with no case database, no per-scheme difference-in-differences, no transferability scoring and no endpoint. Grounds the flagship cordon-traffic estimate in **comparable real-world schemes** instead of the synthetic-city agent model. Ships a curated, auditable database (`cases.py`) of eight real interventions — **London CCZ, Stockholm, Singapore ALS/ERP, Milan Area C, Gothenburg, Oslo toll ring, Ghent circulation plan, Madrid Central LEZ** — each an illustrative/approximate *published* figure (tagged **Observed** but every card flags `source_note: "illustrative… not a live data source"`, honest per SPEC §34) carrying a reported treated change, a background/control trend, study design, identification strength, parallel-trend caveat and a documented city-context similarity. For an input policy it (1) computes a **difference-in-differences** effect per case (treated − control, stripping the city-wide trend the scheme didn't cause), (2) scores **transferability** from auditable factors — intervention family (exact vs cross-pricing), coarse none/low/moderate/high charge strength, revenue-recycling match, city context — pooled by documented weights, (3) pools the *applicable* cases by `identification_strength × transferability` into a central estimate + a **confidence interval that widens** when evidence is weak or analogues disagree, and (4) emits the exact SPEC §7.1 output shape (estimated effect, CI, analogue quality, identification/parallel-trend diagnostics, transferability score). A car ban pools only Ghent; a transit-only policy honestly reports *no comparable scheme* rather than inventing one. Crucially includes an optional **structural cross-check (SPEC §8 honesty)**: on the demo £12 charge the real analogues pool to ≈ **−21%** while the agent-based model predicts ≈ **−93%** → flagged a **"large gap"** with "real flat cordons rarely exceed ~30%… lean on the analogue range as an empirical sanity floor" — turning the ABM's cordon-collapse into an explicitly-uncertain claim rather than false precision. Per-case outcomes Observed, the transferred estimate Estimated; fully deterministic, no LLM in any number (SPEC §7.1/§8/§34); registered in the §33 model registry (14 layers now). Honest `not_modelled`: illustrative headline effects not this city's microdata, single flat cordon headline (no per-corridor/distributional transfer), transfer assumes similar behavioural response, coarse charge bucket not PPP-adjusted. 12 tests; **266 green**, app boots with **35 routes**.]
- [x] Time-Series Layer (SPEC §7.2) [DONE — `backend/app/timeseries/` + `POST /timeseries`: the **last enumerated SPEC §7 hybrid-forecast sub-layer with no dedicated implementation**. §7.1/§7.3/§7.4/§7.5/§7.6/§7.7 all shipped as first-class layers; §7.2 existed only implicitly (a reduced-form elasticity method *inside* the ensemble), while `app/baseline/timeseries.py` merely carries the snapshot forward with a *fixed* growth assumption and a hand-set band — useful as a dashboard reference but **not a fitted statistical model**. §7.2 asks for the layer that treats variables *whose temporal structure is informative* and states plainly: "**Forecast World A first. Then policy models alter the baseline trajectory.**" Now delivered in pure numpy: (1) a **seeded synthetic monthly history** per headline metric (documented DGP — trend + annual seasonality + AR(1) noise, `params.py`), **anchored** so its final month equals the deterministic ABM baseline snapshot (keeping §7.2 continuous with `/simulate`, SPEC §34) — labelled **Simulated** synthetic history, never claimed real; (2) a **structural fit** — OLS local-linear-trend + 12-month seasonal dummies + AR(1) on the residuals — that **forecasts World A first** across the Time-Machine checkpoints with prediction intervals whose variance is **derived from the fit** (regression mean-estimation variance that grows with the extrapolation distance + accumulated AR(1) innovation variance), so the band **widens with horizon honestly** (SPEC §34) rather than by a pasted-on assumption, plus an **honest out-of-sample backtest** (refit on all but a held-out 12-month tail → forecast MAPE) reported alongside the in-sample MAPE; (3) the policy step — the deterministic **ABM Δ(B−A)** shifts the fitted baseline (multiplicative for volumes, additive %-points for shares) to give **World B** ("policy models alter the baseline trajectory" verbatim). On the demo £12 reinvest charge: World-A cordon inflow drifts ~3.3–3.7k trips/day with a fan-out band (95% width 142→425 trips over 10 yr, monotone), World B collapses to ~−92% post-T0 tracking the ABM, a zero-charge no-op leaves World B ≡ World A, share metrics shift additively (0 at T0). Synthetic history **Simulated**, statistical baseline forecast **Estimated**, policy shift **Simulated**; fully deterministic, no LLM in any number (SPEC §7.2/§8/§34); registered in the §33 model registry (**15 layers** now, `llm_touches_numbers=False`, assumptions read live from `DEFAULT_TS_PARAMS`); added to the integration-smoke + determinism-regression standing guards. Honest `not_modelled`: synthetic (not measured) history; univariate per metric (no VAR / exogenous regressors); Gaussian intervals (not a full Bayesian posterior); the behavioural response comes from the ABM, the TS layer only shapes the baseline trajectory + its uncertainty. 9 tests; **275 green**, app boots with **36 routes**.]
- [x] Data Fabric — dataset ingestion & provenance layer (SPEC §4) [DONE — `backend/app/datafabric/` + `GET /data-fabric`: the **dataset ingestion & provenance layer SPEC §4 asks for by name**, previously the last enumerated top-of-pipeline SPEC section with no first-class endpoint. §26 (`/evidence`) traces a single *metric* back through the model; §33 (`/registry`) catalogues the *models*; §32 (`/reproduce`) pins dataset byte-hashes for one run — but nothing published the **dataset-level catalogue with the full §4 provenance record + harmonisation lineage** the spec mandates. Now every dataset the engine reads carries the exact §4 metadata schema (title, publisher, source_url, retrieved_at, geographic_scope, spatial_resolution, time_start/end, frequency, units, variables, license, missingness, revision, confidence, transformation_history) **built live from the file bytes** so it can never drift from what runs: record counts, per-variable dtype/unit/description, **measured missingness** (scans the real records — 0% on the complete synthetic files, reported honestly not assumed) and a content-addressed **`revision` = sha256 of the actual bytes** are all computed on disk at request time; shared scope/licence read from `manifest.json`. Cards for zones / roads / od_pairs / population / buildings (each **Simulated**, publisher = the synthetic generator, real datasets like ONS WU03EW / 3DCityDB / Census microdata listed only as **schema analogues** not live sources, SPEC §34) + the mode-choice **assumption-set** (Estimated, variables introspected live from `DEFAULT_PARAMS`). Publishes the §4 **supported-format contract** (JSON/GeoJSON native; CSV/XLSX adapter-ready; GTFS/gov-APIs/census/budget/Hansard/election/consultation/survey/environmental/admin declared) with honest wiring status, and the §4 **harmonisation pipeline** — geographic joins (all layers on `zone_id`), schema mapping (`dataset.py` typed accessors), unit normalisation (money↔minutes, km/h, veh/hr), population weighting (sample→city rep-factor), provenance tracking (byte hashes + MetricTags), dedup, missing-data treatment marked *implemented* with the code path, and time-alignment / inflation-adjustment / outlier-detection honestly marked **N/A** for a single-snapshot synthetic city (with why) rather than faked. Emits the mandated `input data → transformation → model → assumptions → result` lineage contract. Fabric is **Observed** about the data itself (transparency artifact, not a forecast); fully deterministic, no LLM (SPEC §4/§34); §33 registry left untouched (fabric is data-side, registry is model-side — kept separate like §32/§26). Added to the integration-smoke GET sweep. 9 tests; **284 green**, app boots with **37 routes**.]

- [x] Change-assumptions-and-rerun layer (SPEC §34.10) [DONE — `backend/app/assumptions/` + `GET /assumptions` + `POST /assumptions/rerun`: the **tenth SPEC §34 guardrail** ("users can change assumptions and rerun"), previously the only §34 tenet with no first-class endpoint. The §24 uncertainty engine *sweeps* the documented model assumptions internally and ranks the most-influential one, but nothing let a user **pin an assumption to a chosen value and re-run the deterministic core** to see how much it moves the headline — the natural next click after reading the sensitivity ranking. Now delivered by **reusing the exact `/simulate` pipeline** (`compute_baseline` → `compute_world_b` → `build_world_b_timeline` → `build_delta`) with one or more assumptions overridden — **no new numeric model, no LLM** (SPEC §34). `GET /assumptions` publishes the overridable catalogue (money→time mode-switch elasticity, transit service/fare reinvestment response, central congestion feedback, baseline transit speed, car running cost, transit fare, CO₂ factor) built **live from the running dataclasses** (`DEFAULT_PARAMS`/`DEFAULT_SIM_PARAMS`) and **is the same `ASSUMPTIONS` registry the uncertainty engine sweeps** (single source of truth — a test asserts the two sets are identical, so they can never drift). `POST /assumptions/rerun` takes the compiled Policy DSL + an `overrides` map (+ optional shocks/horizon), re-runs A/B/Δ with those assumptions pinned, and returns: each override echoed with what was actually applied (**out-of-range values clamped to the documented plausible range and flagged `in_range:false`/`clamped:true` with a note** — honest per §34, not silently used), a **per-metric contrast at the horizon** (default-assumption Δ vs overridden Δ + the shift + % of default), the **full overridden Δ trajectory** (replot-ready), and World-A/World-B snapshots. Empty overrides reproduce the default Δ exactly (shift 0); unknown assumption names 404 with the valid list. Fully deterministic (byte-identical across two identical calls) and the overridden Δ still satisfies the §34 widening-uncertainty invariant (guarded). Numbers Simulated, assumptions Estimated; no LLM on the numeric path (SPEC §34.10/§34); added to the integration-smoke GET+POST sweep. 10 tests; **304 green**, app boots with **40 routes**.]

- [x] Baseline World Model composition — the browsable digital twin (SPEC §5 / §28.2) [DONE — `backend/app/world/` + `GET /world`: the **Baseline World Model SPEC §5 names layer-by-layer** and the demo (§28.2 "Baseline Digital Twin: render roads, transit, population cohorts, businesses") renders, previously **unassembled** — `/baseline` returned only aggregate mode/traffic/CO₂/transit *metrics* + time series, `/data-fabric` catalogued the *datasets*' provenance, but nothing composed World A's actual demographic / economic / geographic / institutional / society **structure** the UI twin browses. Now composed deterministically from the synthetic dataset + the baseline ABM into the exact **six SPEC §5 layers**: **Population** (7 985 agents — age bands, household size, income deciles/bands, occupation mix, car/transit mobility %, commute distance, CBD-commuter share, behavioural-prior means), **Economy** (city jobs 154 311, CBD job share, occupations grouped into six documented sectors, mean wage per income band), **Geography** (81 zones/9 CBD + land-use mix, 144 road links/107.9 km/12 cordon-crossing/total capacity by class, 1 250 buildings by type + mean height, commercial-zone & CBD-job business proxy, transit-access coverage), **Environment** (baseline commuter CO₂ daily/annual/kg-per-km straight from the ABM, land-use incl. green space, water/flood-layer presence), **Institutions** (the *modelled* governance agents — 5 parliament + 4 institutional reviewers — as a transparency description, not real bodies), **Society** (income-band opinion priors read live from the cohort model, the six editorial archetypes, the typed diffusion actors with structural role priors). Honours §5's **"smallest sufficient world model"**: `?layers=population,geography` returns just those (unknown layer → 404 with the valid list). Every figure is a count/distribution from the synthetic city (**Simulated**) or an ABM aggregate (**Simulated**) or an Observed/Estimated description of how an agent is modelled — **no LLM, no forecast** (SPEC §34); each layer surfaces its honest `not_modelled` gaps (education/disability/tenure, firms/expenditure/tax, transit route geometry & POIs, air-quality/energy/temperature, individual political affiliation) rather than faking them. Added `load_buildings` to the shared dataset reader; cached so two calls are byte-identical (guarded); added `/world` (full + subset) to the integration-smoke GET sweep so the global provenance-tag guardrail covers it. 10 tests; **323 green** (was 313), app boots with **42 routes** (was 41).]

- [x] Global sensitivity ("tornado") layer (SPEC §24/§26) [DONE — `backend/app/sensitivity/` + `POST /sensitivity`: the **cross-metric explainability layer** that answers the minister's actual question — *"which of your assumptions is the answer resting on?"* — across the **whole dashboard at once**. The §24 uncertainty engine already ranks the most-influential assumptions but only **for a single requested `metric_key`**, and only via an expensive 100-sample Monte-Carlo sweep; nothing gave the cheap, deterministic, *cross-metric* attribution a decision-maker needs ("if you only pin two numbers, pin these"). Now delivered by **one-at-a-time (OAT) sweeps of the exact same `/simulate` pipeline** — for each documented assumption (the **identical `ASSUMPTIONS` set the §24 engine sweeps**, single source of truth) pin it to its plausible **low**, then **high**, edge (all others at default) and re-run A/B/Δ, recording the swing in **every** headline metric's policy effect Δ(B−A) at the horizon. Output is two complementary views: (1) a **per-metric tornado** — assumptions ranked by |swing| around that metric's default Δ, each bar carrying delta-at-low/high, signed swing, direction, % of the default effect, and a scale-free `influence_share` (that metric's sensitivity partitioned across assumptions, sums to 1); (2) a **global driver ranking** — each assumption's `global_score` = mean influence-share across metrics, so drivers with different units compare fairly, with a plain-language leverage note ("dominant driver — pin this first" / "material" / "minor"). On the demo £12 reinvest charge at 5 yr, the answer rests most on the money→time mode-switch elasticity (~29% of dashboard sensitivity) then central congestion feedback (~26%), moving individual effects up to ~44%. Crucially **honest about inert knobs** (SPEC §34): assumptions flat on every metric for a given policy (e.g. the transit-reinvestment knobs under a general-fund or no-charge policy — which the §24 engine also reports as 0 swing, a consistency the tests assert) are flagged `matters:false` / `global_score:0` rather than silently ranked; a no-lever policy yields a "structural, not assumption-driven" headline instead of an invented driver. `not_modelled` states the two real limits plainly: OAT captures **leverage, not likelihood**, and **ignores interactions** (that is exactly what the §24 Monte-Carlo fan on `/uncertainty` samples jointly) — the two layers are complementary, not redundant. Fully **deterministic** (no sampling → byte-identical repeats, guarded), numbers **Estimated** (an attribution derived from the model), **no LLM** on the numeric path; registered in the §33 model registry (**16 layers** now, `llm_touches_numbers=False`) and added to the integration-smoke + audit determinism standing guards. Optional `metric_keys` subset (unknown keys fall back to the full dashboard, never 500). 9 tests; **359 green** (was 350), app boots with **44 routes** (was 43).]

- [x] Citizen View — single-household drill-down (SPEC §17 Citizen View / §31 Agent State) [DONE — `backend/app/citizen/` + `POST /citizen` (+ `GET /citizen/sample`): the **"click a household" drill-down SPEC §17 specifies with an exact worked example** (income, household, home/work, BEFORE POLICY commute+cost, then MONTH 5 / YEAR 2 commute+cost with a "Why?") and the **§31 `Agent State` core data structure**, previously the one macro/micro view with no endpoint — every layer *aggregates* the population (`/microsim`→income deciles, `/world`→distributions, `/public`→cohorts); nothing exposed a single citizen's before/after life. Now delivered as **pure reuse of the existing deterministic models** (no new numeric model, no LLM — SPEC §34): a household's World-A mode/time/cost comes from `mode_options` and its World-B from `policy_mode_options` (the *identical* primitives `/simulate` + `/microsim` use); its policy support from the opinion model's per-agent `_agent_support` (the *identical* function `/public` aggregates — a test pins the far-horizon support == this agent's `/public` contribution). **Staged over the Time Machine** (SPEC §9): the household is interpolated between three structural anchors — World A, behaviour-only World B (charge in force, transit uplift not yet built) and fully-adapted World B — on the *same* behaviour-substitution + transit-ramp curves (`_behaviour_fraction`/`_transit_fraction`) as the aggregate timeline, which reproduces §17's signature **worse-before-better arc** (a reinvestment beneficiary's commute eases 33.6→30.3 min and cost $75→$52.54/mo as the revenue-funded service phases in from ~month 6; a low-income CBD driver's cost jumps $22→$272/mo and support falls to −0.67 as they pay the charge). Emits the household **profile**, a **BEFORE POLICY** World-A snapshot, the **trajectory** across all 8 checkpoints (T0→10y) with commute/cost **bands that widen monotonically** with the horizon (SPEC §9/§34, guarded), the **§31 Agent-State record** at each checkpoint (agent_id/t/location/income/commute_minutes/monthly_transport_cost/policy_support), and a **deterministic "Why?" narrative** tied to the staged model (charge paid, mode switch, transit-lag arc, exemption). Selectors: explicit `agent_id` (404 on miss) or `select` archetype (representative CBD car driver / most_burdened / biggest_loser / biggest_winner / median; 422 on unknown); `GET /citizen/sample` returns a policy-independent household picker spanning all five income bands. Deterministic (byte-identical repeats — guarded), numbers Simulated; registered in the §33 model registry (**17 layers** now, `llm_touches_numbers=False`) and added to the integration-smoke (POST + `/citizen/sample` GET) + determinism-regression + `scripts/audit.py` standing guards. 11 tests; **370 green** (was 359), app boots with **46 routes** (was 44).]

- [x] Business View — single-firm drill-down (SPEC §17 Business View) [DONE — `backend/app/business/` + `POST /business` (+ `GET /business/sample`): the **micro counterpart to the Citizen View that SPEC §17 asks for by name** — *"Click a firm. Show: footfall, labour accessibility, deliveries, costs, revenue proxy, adaptation decisions. This makes the model explainable at both macro and micro levels."* — the last §17 view with no endpoint (Citizen View shipped last run; every other layer aggregates firms into zone/economy totals). Now delivered as **pure reuse of the existing deterministic models — no new numeric model, no LLM** (SPEC §34): a firm's **labour accessibility** is the commute generalized cost of *its own workers* (the synthetic commuters whose `work_zone` is the firm's zone) under the *identical* `mode_options`/`policy_mode_options` mode-choice model `/simulate` + the Citizen View use (a test recomputes the World-A mean commuter GC agent-by-agent and pins it == the index base); its **footfall / deliveries / cost / revenue** reuse the *same economic coefficients* as `/economy` (`EconParams`: spend-per-visit, freight pass-through, `cbd_trip_avoidance_fraction`, `pedestrianisation_retail_uplift`) plus a few strictly firm-level allocation ratios in `params.py` (customer-per-worker by sector, deliveries-per-1000 m², storey height — all documented Estimated). **Firms come from the building stock**: each commercial building in `data/city/buildings.geojson` (office/tower/podium/lowrise/mixed/industrial → mapped sectors; residential/park excluded) is a firm, its jobs allocated from the zone's `jobs` total by gross-floor-area share (a test asserts allocated jobs never exceed the zone total). **Staged over the Time Machine** (SPEC §9): footfall/labour-access/cost/revenue interpolate the same three structural anchors (World A, behaviour-only World B, fully-adapted World B) on the *same* `_behaviour_fraction`/`_transit_fraction` curves as the aggregate timeline — so a firm's labour access dips as commute costs rise then recovers as the reinvested transit uplift lands. Emits the firm **profile** (sector/zone/floors/floor-area/estimated-jobs), a **BEFORE POLICY** World-A snapshot, the **trajectory** across all 8 checkpoints (T0→10y) with footfall/cost/revenue **bands that widen monotonically** (SPEC §9/§34, guarded), a **labour-accessibility index** (100 = baseline ease; <100 = worse), **daily deliveries** + the **charge-pass-through operating cost** a central firm pays on its inbound deliveries, a **revenue proxy** (footfall × spend — clearly Estimated, ratio-meaningful only), a **net-revenue-proxy change %**, deterministic **adaptation decisions** (absorb/consolidate deliveries, lean into pedestrian footfall under a car ban, staff transit passes under labour-access pressure, relocation-risk flag past a revenue-drop threshold, minimal exposure for outer firms) and a **"Why?" narrative**. On the demo £12 reinvest charge a central retail firm sees ~94% of car arrivals deterred, ~$173k/yr added delivery cost and labour access falling to ~81, netting a modelled −12% revenue proxy. Selectors: explicit `firm_id` (404 on miss) or `select` archetype (representative central retailer / most_exposed / biggest_footfall_loss / pedestrian_winner / largest; 422 on unknown); `GET /business/sample` returns a policy-independent firm picker spanning sectors. Deterministic (byte-identical repeats — guarded), physical drivers Simulated → firm translation Estimated (`output_tag=Estimated`, SPEC §8); registered in the §33 model registry (**18 layers** now, `llm_touches_numbers=False`) and added to the integration-smoke (POST + `/business/sample` GET) + determinism-regression + `scripts/audit.py` standing guards. Honest `not_modelled`: allocated (not measured) jobs/floor-area/deliveries; customers/deliveries not agent-modelled (revenue is a proxy); footfall captures deterred car customers + pedestrianisation amenity only; adaptation decisions are deterministic exposure rules, not an optimised firm-response sim. 11 tests; **381 green** (was 370), app boots with **48 routes** (was 46).]

- [x] Low-emission zone as a distinct mechanism, not an aliased congestion charge (SPEC §7.5/§34) [DONE — `backend/app/simulation/levers.py` + `model.py`: the DSL has always named **`low_emission_zone`** as a first-class intervention family, but the levers layer silently aliased it into `_PRICING_TYPES` alongside road-pricing/parking-levy — so an LEZ was modelled as a **flat cordon charge on every car**, telling a minister it cuts traffic exactly as much as a congestion charge. That is wrong on the mechanism and dishonest per §34: an LEZ charges only **non-compliant** vehicles and its dominant lever is **fleet turnover to cleaner vehicles** (lower CO₂/km), *not* a large mode shift. Now given its own deterministic branch in `derive_levers`: (1) a **fleet-expected charge** = the amortised amount × `lez_noncompliant_share` (only the dirty share of the fleet pays, so the behavioural signal — and the resulting mode shift — is a documented fraction of an equivalent flat charge, while total charge revenue is conserved: share × full charge); (2) a **fleet-cleanup lever** — a new World-B-only `co2_factor_multiplier` = `(1−share) + share×lez_clean_factor_ratio`, blending the compliant fleet's baseline factor with cleaner replacement vehicles, so the emissions-intensity falls even for drivers who keep driving. Both surfaced as self-describing §7.5 `BehaviouralRule`s (`lez_charge`, `lez_fleet_cleanup`) for the Evidence Drawer; the CO₂ cut is explicitly modelled as a **CO₂ proxy for the tailpipe (NOx/PM) turnover an LEZ actually targets**, so it is deliberately modest — an honest "cleaner fleet, similar traffic" instead of false precision. **World A is never touched** and every non-LEZ policy keeps `co2_factor_multiplier == 1.0`, so all existing numbers are **byte-identical** (charge/pedestrianisation regression pinned by a test). On the demo £12 policy the two now diverge correctly: **LEZ** car share 57.2→49.1%, CO₂ factor 0.192→0.163 (−15% cleaner fleet), 2.84→2.19 tCO₂/day; **congestion charge** car share 57.2→37.7% at an *unchanged* 0.192 factor, 2.84→1.76 tCO₂/day — the LEZ keeps more cars on the road but cuts emissions intensity, the charge cuts km. New SimParams (`lez_noncompliant_share` 0.25, `lez_clean_factor_ratio` 0.40) are documented Estimated assumptions **surfaced live in the §33 registry** so the manifest can't drift. Deterministic, no LLM on the numeric path (SPEC §7.5/§34). 10 tests; **415 green** (was 405), audit PASS, app boots with **52 routes**.]
- [x] Workplace parking levy as a distinct mechanism, not an aliased congestion charge (SPEC §7.5/§34) [DONE — `backend/app/simulation/levers.py` + `registry/model.py`: the sibling gap the LEZ fix exposed — `parking_levy` was still the *last* intervention type silently aliased into `_PRICING_TYPES` alongside road-pricing, so a workplace parking levy produced **byte-identical** traffic/emissions numbers to a full cordon charge for the same amount. That is wrong on the mechanism and dishonest per §34: a WPL (e.g. Nottingham's) is levied on the **employer per parking space**, who absorbs some of it and passes only a fraction through to the commuter, so the per-commuter behavioural signal — and the mode shift — is proportionately **smaller** than a road-pricing cordon that every entering vehicle pays in full. Now removed from `_PRICING_TYPES` and given its own deterministic branch in `derive_levers`: `charge_per_one_way = (amount / trips_per_day) × parking_levy_passthrough_share`, surfaced as a self-describing §7.5 `BehaviouralRule` (`parking_levy_charge`, distinct from `cordon_charge`/`lez_charge`) for the Evidence Drawer. Unlike an LEZ it leaves `co2_factor_multiplier == 1.0` (it cuts emissions purely by cutting car-km), and because it sets a positive charge the revenue→transit reinvestment lever still engages unchanged. The three pricing mechanisms now sit in an honest, distinct ordering on residual car share — **LEZ (0.25 non-compliant) < parking levy (0.55 pass-through) < cordon charge (full)** — instead of two of them collapsing onto identical numbers. **World A is never touched and road pricing is byte-identical** (pinned by a regression test asserting the cordon charge == amount/trips_per_day with the levy split out). New SimParam `parking_levy_passthrough_share` (0.55, documented Estimated) is **surfaced live in the §33 registry** so the manifest can't drift. Deterministic, no LLM on the numeric path (SPEC §7.5/§34). 10 tests; **425 green** (was 415), audit PASS, app boots with **52 routes**.]
- [x] Standalone transit investment as a distinct supply-side mechanism, not a silent no-op (SPEC §7.5/§9/§34) [DONE — `backend/app/simulation/levers.py` + `model.py` + `registry/model.py`: `transit_investment` is a named DSL intervention family, but with no charge and no ban it previously **hit no lever branch at all** — so a "invest in buses/transit" policy left World B **byte-identical to World A**, a silent no-op a minister would misread as "transit investment does nothing". Its real, honest lever is **supply-side**: a service package (a fare cut + effective-speed uplift) pulls commuters over *voluntarily*. Now given its own deterministic branch in `derive_levers`: the same fare-cut / speed-uplift the revenue-reinvestment lever uses, scaled by an explicit `transit_investment_intensity` (0.5, Estimated) **rather than by the currency amount** — the model has no cost→service function, so a £→service elasticity would be false precision (SPEC §34); a test pins the uplift is identical whether or not a £ amount is stated, and that intensity is a tunable assumption (0 → no lever). Because the uplift lands via the transit multipliers, the **short-run anchor (reinvestment off) leaves it neutral** and the long-run anchor applies it, so a pure transit investment **ramps in over the horizon** like any capacity build instead of switching on at T0 (SPEC §9) — pinned by short-run==baseline / long-run>baseline tests. Honest-finding guardrail: with **no stick**, the pull mostly reshuffles walk→transit rather than removing cars, so a test asserts the car drop is no larger than the transit gain (the model must not overstate "pull without push"). Leaves `co2_factor_multiplier == 1.0` (cuts emissions only via mode shift, not fleet cleanup — distinct from an LEZ). New SimParam `transit_investment_intensity` **surfaced live in the §33 registry**; metrics Simulated + deterministic (pinned). Deterministic, no LLM on the numeric path (SPEC §7.5/§34). 10 tests; **435 green** (was 425), audit PASS, app boots with **52 routes**. *(Recovered: implemented + green in a prior engine run that exited before committing; committed here alongside the active-hours item.)*]
- [x] Charge operating-hours actually bite — `active_hours` scales the charge, a peak-only scheme is no longer an all-day one (SPEC §7.5/§34) [DONE — `backend/app/simulation/levers.py` + `registry/model.py`: `intervention.active_hours` (the daily charging window) was **parsed by both the LLM and the rule compiler, stored in the DSL, and surfaced as provenance — but never touched a single number.** A congestion charge set to run **07:00–10:00** (AM peak only) produced **byte-identical** traffic / emissions / revenue to one running **07:00–19:00** all day, because the derived `charge_per_one_way` ignored the window entirely. That is dishonest per §34: it would tell a minister a 3-hour peak-only scheme cuts traffic and raises revenue exactly as hard as a 12-hour all-day one, when trips outside the charging window face no price signal and don't shift. The charged event in this model is the **inbound CBD-bound car leg**, which clusters in the morning peak, so the honest correction is to scale the charge by how much of that **inbound AM commute peak** the operating window actually covers: `_active_hours_coverage` = overlap(active_hours, inbound-peak) ÷ peak-length ∈ [0,1], and every pricing branch now multiplies its per-trip signal by it. Because `charge_per_one_way` is the **single lever every downstream layer reads** (mode choice, microsim, economy, opinion, business, **and revenue → transit reinvestment**), scaling it at the source propagates a consistent, honest weakening through the whole engine — a narrow-window charge shifts fewer drivers **and** funds less bus service, instead of both silently assuming all-day operation. Applies uniformly to all three money-charge mechanisms (cordon / LEZ / parking-levy); the LEZ<levy<cordon ordering is preserved under partial coverage (common factor, pinned by a test). Surfaced as its own self-describing §7.5 `BehaviouralRule` (`active_hours_coverage`) **only when it bites** (coverage < 1.0), so the default all-day window adds no rule and changes no number. New SimParams `commute_inbound_peak_start`/`commute_inbound_peak_end` (07:00–10:00, documented Estimated) are **surfaced live in the §33 registry** so the manifest can't drift. Degenerate/unparseable windows fall back to coverage 1.0 (an operating window we can't interpret must not invent or erase a mode shift). **World A untouched, and every existing charge is byte-identical** because the default 07:00–19:00 window fully contains the peak → coverage 1.0 (regression pinned by a test asserting the all-day cordon charge == amount/trips_per_day and emits no coverage rule). Deterministic, no LLM on the numeric path (SPEC §7.5/§34). 9 tests; **444 green** (was 435), `scripts/audit.py` PASS, app boots with **52 routes**.]
- [x] Active-travel revenue reinvestment actually bites — spending charge revenue on walking/cycling is no longer a silent no-op (SPEC §7.5/§9/§34) [DONE — `backend/app/simulation/levers.py` + `model.py` + `policy/rules.py` + `registry/model.py`: `revenue_allocation.active_travel` is a **first-class field on the Policy DSL** — extractable by the LLM, normalised by the compiler's allocation-sums-to-1 guardrail, echoed in provenance — but it **never touched a single number**. The revenue→service lever read **only** `revenue_allocation.public_transport`; a policy that spent 100% of its charge revenue on **protected cycle lanes, wider pavements and secure cycle parking** produced **byte-identical** traffic/emissions to one that banked every penny in the general fund. Dishonest per §34: it would tell a minister that building active-travel infrastructure does **literally nothing** to mode share or emissions. Now given its own deterministic lever mirroring transit reinvestment: revenue allocated to active travel raises an `active_travel_speed_multiplier` = `1 + share × active_travel_max_speed_gain` (0.20, Estimated) that scales **both** the effective active-travel speed **and** the maximum walkable/cyclable commute distance in World B (segregated infrastructure both speeds up existing foot/bike trips *and* brings slightly longer commutes into range), pulling the **nearest-margin short-trip car and transit commuters** onto active travel — on the demo £10 charge with full active-travel reinvestment, walk 3051→5036, car 3012→1812, daily vehicle-km 9181→7229. Honest guardrails, matching the transit-reinvestment precedent: (1) it **only engages when the charge/ban actually raises revenue** (`charge_per_one_way>0 or car_banned`), so a no-revenue policy can't invent a mode shift; (2) it rides the **same reinvestment gate** — neutral in the short-run anchor, present in the long-run one — so the infrastructure **ramps in over the horizon** (SPEC §9) rather than switching on at T0 (pinned: short-run==banked-baseline); (3) the multiplier is an explicit **Estimated** assumption, **not derived from the £ amount** (the model has no cost→infrastructure function, so a £→service elasticity would be false precision — §34); (4) it **co-exists** with transit reinvestment — a split allocation (e.g. 60% transit / 40% active travel) engages both service levers independently. Also taught the **rule-based compiler** to recognise active-travel allocation ("spend 40% on cycle lanes / walking / footpaths / pavements") so the mechanism is reachable and testable **without an LLM** (transit is still matched first, so existing transit-percentage parses are unchanged). New SimParam `active_travel_max_speed_gain` **surfaced live in the §33 registry** so the manifest can't drift; share clamped to [0,1]. **World A untouched and every existing (active-travel-free) policy byte-identical** because the default allocation carries `active_travel=0.0` → multiplier 1.0, adds no rule and changes no number (regression pinned). Deterministic, no LLM on the numeric path (SPEC §7.5/§9/§34). 11 tests; **459 green** (was 448), `scripts/audit.py` PASS, app boots with **53 routes**.]
- [x] Stated equity constraint is tested against the numbers, not just asserted (SPEC §7.3/§34) [DONE — `backend/app/microsim/schema.py` + `model.py`: a policy can declare `constraints.max_low_income_burden_increase_pct` — a hard cap the minister sets on how much the charge may raise low-income households' burden. The rule compiler **and** the LLM both extract it, it is echoed as a reviewable assumption, and the Equity Advocate persona even argues it "must be monitored against the modelled distributional outcome, not just asserted" — yet **nothing ever checked it against the numbers**. A constraint you never test is theatre (SPEC §34). The microsim now computes the modelled World-B out-of-pocket charge burden on the lowest-income decile (the baseline has no cordon charge, so this **IS** the increase the cap governs) and attaches a `ConstraintCheck` reporting the cap, the modelled burden, `satisfied`, the signed margin (headroom vs overshoot), and a plain-language reading that **honestly flags a violation** ("EXCEEDING the stated cap by Npp — the policy violates its own equity constraint") rather than hiding it. Stays `None` when the DSL declares no cap, so **every existing policy is byte-identical** (regression pinned). A **low-income exemption drives the modelled burden to 0.0 and satisfies any cap** — the honest reward for designing the exemption. Reachable **without an LLM** (the rule compiler extracts the cap from NL — pinned by a parse→report test). Provenance Simulated; deterministic, no LLM on the numeric path (SPEC §7.3/§34). 6 tests; **465 green** (was 459), `scripts/audit.py` PASS, app boots with **53 routes**. *(Recovered: implemented + green in a prior engine run that exited before committing; committed here.)*]
- [x] Scenario-presets catalogue — the discoverable menu of canonical demo policies (SPEC §3/§27/§28) [DONE — `backend/app/scenarios/` + `GET /scenarios` (+ `GET /scenarios/{scenario_id}`): the engine now serves 58 routes but **every one that takes a policy demanded the caller author or compile a Policy DSL first** — there was no discoverable list of the demo's *own* canonical scenarios a judge or the UI could load with one click. The whole keyless-example surface hard-codes "the canonical §28 demo charge"; nothing exposed a *menu*. This layer is that menu: **6 curated policies spanning 5 distinct intervention families** — CBD congestion charge (reinvested in buses), the same charge routed to the general fund (the contrast that shows *reinvestment* drives the transit gains, not the charge alone), CBD pedestrianisation, a central low-emission zone, a workplace parking levy, and pure bus-network investment (the "carrot without the stick" baseline). **Pure inputs — no numeric model, no LLM in any figure (SPEC §34):** each card carries only its natural-language prompt + the optimiser objective/constraints; everything structured (the DSL, the intervention `family`, the reviewable assumptions) is produced by running the **real `compile_policy`** at build time, so a card **can never disagree with `POST /policy/compile`** — a test recompiles every prompt and pins `card.compiled == compile_policy(text)` field-for-field, and `family` is *derived from* the compiled DSL's intervention type rather than hand-declared (a second test pins each id→family and asserts they agree). Every card ships **two copy-paste-runnable bodies** — `simulate_body` (`{"policy": <compiled DSL>}` for `/simulate` and any endpoint taking a compiled policy) and `answer_body` (`{"text","objective","constraints"}` for the composed-answer endpoints `/run` / `/north-star` / `/brief`) — and a test POSTs **both** for **every** scenario and asserts 200, so the menu is guaranteed executable end-to-end, not decorative. The library is tagged **Observed** (it lists curated inputs, like `/registry` / `/capabilities`); per-card `compiled.provenance` is **Generated** (structuring, not simulation) — a recursive walk asserts no payload field leaks a non-§34 tag. `GET /scenarios/{scenario_id}` returns one card or **404 echoing the valid ids**; deterministic (byte-identical on repeat — guarded); root `/` now advertises `scenarios: /scenarios`. Reconciled with the §33 capability manifest: both routes carded in `capabilities/catalogue.py` (`undocumented_routes`/`phantom_cards` stay empty — the standing reconciliation test would fail otherwise), and the keyless-example derivation now **excludes parameterised paths** (`/scenarios/{scenario_id}` needs an argument, so it is not a no-body GET — a one-line correctness fix in `capabilities/model.py` that also future-proofs the manifest). Added to the whole-surface `scripts/audit.py` GET sweep (served-200 + provenance-tag). Pure additive — no existing `backend/app/**` behaviour changed. 8 tests; **497 green** (was 489), `scripts/audit.py` PASS, `scripts/capabilities.py` PASS, app boots with **60 routes** (was 58). No frontend/shared files touched.]

## Hardening (guard the whole engine, not one layer)
- [x] Full-pipeline integration smoke test (SPEC §3/§34) [DONE — `backend/tests/test_integration_smoke.py`: the one test that drives the **whole engine through its HTTP surface** exactly as the UI does, to catch the cross-layer contract drift unit tests miss (a shared Policy-DSL / `Shocks` / metric-key change that silently 500s a downstream endpoint whose own test file never re-runs against the live app). Compiles the SPEC §28 demo policy **once** (NL→DSL via `/policy/compile`), then feeds that single compiled DSL into **all 33 routes** (5 GET + 22 POST including `/simulate/amend`, `/compare`, `/optimise`, `/uncertainty`, `/backtest`) and asserts each returns 200. Enforces the SPEC §34 guardrails **globally in one place**: recursively walks every response and asserts every field named `provenance` references at least one allowed tag (Observed/Estimated/Simulated/Generated) — accepting both bare enums ("Simulated") and descriptive sentences that embed them ("ABM anchors Simulated; dynamics coefficients Estimated"), which is what surfaced the two valid styles across layers; plus the core `/simulate` is Simulated with Δ=B−A pointwise, the compiler output is Generated, **every** registry model asserts `llm_touches_numbers==False` (no LLM in the numeric path), and `/media` carries the mandatory SIMULATED banner. If any endpoint 500s or drops its tag the night before the demo, this single test goes red. Pure test-track addition — no `backend/app/**` behaviour changed. 5 tests; **232 green**, app boots with **33 routes**.]
- [x] Determinism regression guard (SPEC §24/§34) [DONE — `backend/tests/test_determinism_regression.py`: enforces SPEC §34's central anti-"AI astrology" claim directly on the HTTP surface — calls each of the 12 numeric layers **twice with an identical body** and requires **byte-identical JSON** (`/simulate`, `/spatial`, `/microsim`, `/economy`, `/dynamics`, `/sdg`, `/ensemble`, `/stress-test`, `/diffusion`, `/public`, `/parliament/failure-modes`, seeded `/uncertainty`). Stronger than "returns 200": catches hidden non-determinism that would erode the reproducibility claim — an unseeded RNG, dict/set iteration order leaking into output, wall-clock bleeding into a number, float reduction-order drift. `/reproduce` (§32) *asserts* determinism per-run by re-hashing; this *enforces* it across the surface. Plus a seeded Monte-Carlo check: the one stochastic layer (`/uncertainty`) must still be exactly reproducible for a fixed seed (§24). LLM-prose layers (`/media`, `/press-conference`, `/parliament/debate`) excluded by design — their prose is Generated. Test-track only; no `backend/app/**` change. 2 tests; **234 green**, app boots with **33 routes**.]
- [x] Uncertainty-widens-with-horizon guard (SPEC §34) [DONE — `backend/tests/test_uncertainty_widening_guard.py`: the **third** §34 invariant, previously guarded only per-layer, now enforced globally on the forecast surface the UI actually plots. The determinism guard proves *reproducible*, the integration smoke proves *tagged + LLM-free*; this proves *honest about the future* — a model that quotes the same ±band at 10 years as at T0 is claiming decade-out certainty it cannot have, exactly the "AI astrology" §34 forbids. Recursively walks `/simulate` and `/simulate/amend` (the parliament loop plots the amended world too), finds **every** band-series wherever it nests (world_a / world_b / delta / amended), and asserts on each: (1) band width is **monotonically non-decreasing** with horizon — a mutation test confirms a narrowing band trips it, so it is not a no-op; (2) the far horizon is **strictly wider** than T0 for every base-forecast series, so a *flat* band (which satisfies monotonicity degenerately) also fails — the fan chart must actually fan out; (3) `low ≤ value ≤ high` everywhere, so the plotted line never falls outside its own shaded interval. Test-track only; no `backend/app/**` change. 3 tests; **237 green**, app boots with **33 routes**.]
- [x] Cross-layer mode-choice consistency guard (SPEC §7.3/§7.7/§34) [DONE — `backend/tests/test_cross_layer_consistency.py`: the **fourth** standing guard, closing a real cross-layer gap the existing three don't cover. Several layers advertise they read the *same deterministic mode-choice model* as `/simulate` — the spatial layer (§7.7: "can never disagree with the ABM's mode split", loads only agents who still `choose_mode`==car) and the microsim layer (§7.3: "same mode-choice model as /simulate", but reached via `mode_options`/`policy_mode_options`+`pick_mode` rather than the `choose_mode`/`choose_mode_policy` wrappers). Every per-layer test checks its layer **in isolation**, so nothing would notice if a refactor let these paths drift — a change to `choose_mode` that `pick_mode(mode_options(...))` no longer mirrors, or a layer sampling a different population — silently building a road network or a who-gains/who-loses table on a *different* mode split than the headline `/simulate` numbers beside it (exactly the §34-forbidden cross-layer contract drift). The guard computes the canonical World-A/World-B car-commuter set from the population once, then over **four structurally-distinct policies** (reinvesting charge, general-fund charge, pedestrianisation, behavioural no-op) asserts: (1) `/simulate` (`compute_baseline`/`compute_world_b`) reports the exact canonical car counts; (2) the microsim primitives reproduce `choose_mode`/`choose_mode_policy` **agent-by-agent** (identical per-person decision, not just equal totals); (3) `_car_demand` loads exactly the canonical car set onto the network — its peak-hour trips equal the canonical count × the documented rep-factor × peak-share (mutation-checked: a one-driver drift is detectable); (4) a zero-charge no-op is behaviourally identical across layers; (5) all layers partition the one synthetic population. Test-track only; no `backend/app/**` change. 14 tests; **251 green**, app boots with **33 routes**.]

## Orchestration (compose the engine for the killer demo)
- [x] Grand counterfactual — canonical A/B/C/D comparison (SPEC §21/§22) [DONE — `backend/app/simulation/counterfactual.py::compare_grand` + `POST /compare/grand`: the **§21 spec names four worlds by role — A (baseline), B (intervention), C (opposition amendment), D (URBAN-optimised policy)** — but `/compare` only accepted *arbitrary caller amendments* as C/D/E…; nothing composed the canonical four-way set, and in particular nothing wired the §22 optimiser's recommendation in as **World D** or auto-derived the opposition amendment as **World C**. `compare_grand` now builds the exact §21 quartet in one call: **World A** baseline, **World B** = the compiled policy, **World C** = the opposition amendment (caller-supplied `amendment`, else the deterministic equity-first default extracted into `simulation/amendment.py::propose_opposition_amendment` — a flat charge → *exempt low-income commuters*, already-equitable+reinvesting → no World C), **World D** = the §22 optimiser's **best-balanced** recommendation (falling back through largest-emissions-cut / most-equitable / cheapest / frontier), its `CandidateConfig` **rebuilt into a Policy DSL** and **re-simulated through the identical deterministic path** as every other world so the numbers can't disagree with the standalone optimiser or `/simulate` (SPEC §34). **No new numeric model, no LLM** — pure composition of `compute_baseline`/`compute_world_b`/`build_world_b_timeline`/`build_delta` (shared with `/compare` via an extracted `_assemble_comparison` helper — the plain endpoint is byte-for-byte unchanged, guarded by a test asserting its response carries no `derivation`) + `apply_amendment` + `optimise_policy`. Returns the full A/B/C/D headline table (baseline never omitted, every row one cell per world, Δ = world − baseline — guarded) plus a `derivation` record exposing exactly how C and D were chosen (amendment source/rationale; optimiser objective/constraints echo, selection slot, chosen policy id + config, feasibility, candidate counts) for auditability. Deterministic (two identical calls byte-identical — guarded); numbers Simulated. Added to the integration-smoke route sweep. 9 tests; **313 green**, app boots with **41 routes**.]
- [x] North-Star answer — the §37 minister's answer as one endpoint (SPEC §37) [DONE — `backend/app/northstar/` + `POST /north-star`: SPEC **§37 "North-Star Experience"** names *the* URBAN interaction verbatim — a minister asks *"What happens if we implement this?"* and URBAN answers with a **fixed 15-line narrative** (baseline → historical analogues → mechanisms → median outcome → uncertainty → who benefits → who loses → most-likely failure → opposition's strongest argument → opinion evolution → media narratives → three risk-reducing amendments → each amendment's effect → the best-fit configuration → every assumption + piece of evidence). The `/run` orchestrator composes the **§28/§29 killer-demo** beat sequence (compile→sim→public→parliament→amendment→media); §37 is a **different, larger composition** — an explicit ministerial *answer* that adds the analogue layer, the uncertainty fan, the who-gains/who-loses microdata, the ranked failure register, the opinion-diffusion arc, the optimiser's best-fit config and the full assumption/guardrail ledger that `/run` never assembled. Now delivered as **pure orchestration — no new numeric model, no LLM in any figure** (SPEC §34): every one of the 15 sections **embeds the same object the standalone endpoint returns** — `sim.world_a.snapshot` (§37.1) & `event_ledger` (§37.3) from `/simulate`, `run_analogues` (§37.2), a snapped-horizon headline dashboard = `/simulate` Δ (§37.4, a test pins tile==delta), `run_uncertainty` on the flagship cordon metric (§37.5, 80 seeded MC draws), `build_microsim_report` winners/losers/regressivity (§37.6/7), `build_failure_register` (§37.8), the most-confident non-supporting `/parliament/debate` argument (§37.9, a test pins it's a real debate contribution), `run_diffusion` opinion arc (§37.10), `run_media` SIMULATED feed (§37.11), up to **three deterministically-derived risk-reducing amendments** each re-simulated via `compare_amendment` (§37.12/13 — flat-charge→exempt low-income for regressivity, <100% reinvest→full reinvest for crowding, any charge→half-strength phase-in for backlash), the `/optimise` best-balanced pick for the caller's objective/constraints (§37.14), and the **live `/registry` assumption index + §34 guardrails** (§37.15, asserts `llm_touches_numbers=False`) — so the minister's answer **can never disagree** with the tabs behind it. Each section also carries a deterministic one-sentence `lead` read straight off those numbers + a provenance tag (Simulated/Estimated/Observed/Generated). Deterministic (two identical calls byte-identical — guarded); 422 when neither `text` nor `policy` supplied; mirrors `/run`'s NL-compile-or-precompiled input. Added to the integration-smoke route sweep (global provenance-tag + no-LLM guardrail now covers it). Pure additive composition — no existing `backend/app/**` layer changed. 12 tests; **340 green**, app boots with **43 routes**.]
- [x] Scenario orchestrator — one-call killer-demo pipeline (SPEC §28/§29) [DONE — `backend/app/scenario/` + `POST /run`: the **composition endpoint the killer demo (§29) is literally a script for**, previously absent — every layer was a *separate* HTTP call, so the UI (and a judge poking the API) had to fan out ~7 requests and trust that the parliament, dashboard, amendment and media all rebuilt the *same* policy/sim underneath. `/run` runs the whole §29 narrative **once** — compile (NL→DSL, or accept a pre-compiled `policy`) → `/simulate` A/B/Δ+event-ledger → cohort `/public` reaction → model `/parliament` debate → an **auto-derived amendment** re-simulation → `/media` feed — and returns them in one mutually-consistent envelope. Introduces **no new numeric model** (SPEC §34): it *reuses the exact endpoint services* (`simulate()`, `compute_public_opinion`, `run_debate`, `compare_amendment`, `run_media`) so the composed numbers are byte-identical to the standalone endpoints — a test pins `headline == /simulate delta at the horizon` and `net_support == /public`, i.e. the sections **can never disagree**. The **auto-amendment mirrors the §29 beat deterministically**: a flat charge with no income exemption → *exempt low-income commuters* (`auto:equity`, cites the regressive-burden rationale); already-equitable+full-reinvest → no amendment; caller may override with an explicit `amendment` (`source:'caller'`). Emits a **§29 narrative** (six timecoded beats each pointing at the response section that backs it) and a **headline dashboard** (cordon traffic / commuter CO₂ / car share / transit ridership / peak crowding at the chosen horizon, default Year 2) with per-tile direction + Δ band — the exact "Traffic↓ Emissions↓ Crowding↑" tiles the demo scrubs to. Numbers Simulated, debate/media prose Generated, no LLM in any figure; deterministic numeric sections (a test asserts two identical runs give byte-identical `simulation`/`headline`/`net_support`/amendment `comparison`); 422 when neither `text` nor `policy` supplied. Added to the integration-smoke route sweep. Pure additive composition — no existing `backend/app/**` layer changed. 10 tests; **294 green**, app boots with **38 routes**.]
- [x] Keyless `GET /run/example` — the killer-demo pipeline with no request body (SPEC §28/§29/§34) [DONE — `backend/app/routers/run.py`: the §29 orchestrator (`POST /run`) is the demo, but the *keyless* surface every other judge-facing artifact already offers was missing — `GET /brief/example` and `GET /backtest/example` let a judge (or the UI's first paint) pull the canonical answer with **zero request body**, while the single most important endpoint, `/run`, still required a hand-built `RunRequest`. `GET /run/example` closes that gap: it orchestrates the **canonical §28 demo congestion charge** (the same `_DEMO_TEXT` the brief example uses) end-to-end through the *identical* `run_scenario()` service — compile → simulate → public → parliament → auto-amendment → media — and returns the full §29 narrative envelope. **Pure additive routing — no new numeric model, no LLM in any figure** (SPEC §34): it is a thin `run_scenario(RunRequest(text=_DEMO_TEXT))` wrapper, so a test pins its `simulation`/`headline`/`net_support`/amendment `comparison` **byte-identical** to `POST /run {"text": _DEMO_TEXT}` — the example can never drift from the real endpoint. Guarded by 2 new tests (full-pipeline-with-no-body + example-equals-posting-the-demo-text); the whole-surface `scripts/audit.py` GET sweep now covers it (served-200 + provenance-tag). **467 green** (was 465), `scripts/audit.py` PASS, app boots with **54 routes** (was 53). No frontend/shared files touched.]
- [x] Keyless `GET /north-star/example` — the §37 minister's answer with no request body (SPEC §37/§34) [DONE — `backend/app/routers/northstar.py`: completes the keyless judge-facing surface across all three composed answer endpoints. `GET /brief/example`, `GET /backtest/example` and (M47) `GET /run/example` already let a judge pull the canonical artifact with no body; the §37 North-Star answer — the endpoint the Minister's Brief itself delegates to — still required a hand-built `NorthStarRequest`. `GET /north-star/example` composes the full §37 fixed-narrative answer for the canonical §28 demo congestion charge through the *identical* `run_north_star()` service, using the **same inputs `GET /brief/example` renders** (`text` + objective `reduce_transport_emissions_pct:20` + constraint `max_low_income_burden_increase_pct:2`) so the two examples describe the same run. **Pure additive routing — no new numeric model, no LLM in any figure** (SPEC §34): a thin `run_north_star(NorthStarRequest(...))` wrapper; a test pins the example **byte-identical** (`json.dumps(sort_keys=True)`) to `POST /north-star` with those same inputs, so it can never drift from the real endpoint, and a second test asserts the fixed 15-line §37 narrative + non-empty `median_outcome` with no body. Whole-surface `scripts/audit.py` GET sweep now covers it (served-200 + provenance-tag). 2 tests; **469 green** (was 467), `scripts/audit.py` PASS, app boots with **55 routes** (was 54). No frontend/shared files touched.]
- [x] Keyless `GET /compare/example` — the §21 four-world A/B/C/D comparison with no request body (SPEC §21/§22/§34) [DONE — `backend/app/routers/compare.py`: completes the keyless judge-facing surface for the **most fundamental §21 view** — *"never show intervention metrics without the baseline"*. The composed answer endpoints (`/brief/example`, `/run/example`, `/north-star/example`) and `/backtest/example` all let a judge pull the canonical artifact with no body, but `POST /compare/grand` — the canonical **World A (baseline) / B (intervention) / C (opposition amendment) / D (URBAN-optimised)** quartet — still required a hand-compiled `PolicyDSL`. `GET /compare/example` compiles the canonical §28 demo congestion charge and runs the *identical* `compare_grand()` service the POST endpoint uses (deriving World C from the deterministic opposition rule and World D from the §22 optimiser's best-balanced pick), with the **same objective `reduce_transport_emissions_pct:20` + constraint `max_low_income_burden_increase_pct:2`** the brief/north-star examples optimise against — so all keyless examples describe one consistent demo run. **Pure additive routing — no new numeric model, no LLM in any figure** (SPEC §34): a test pins the example **byte-identical** (`json.dumps(sort_keys=True)`) to `POST /compare/grand` with those same compiled inputs, so it can never drift from the real endpoint; a second test asserts World A baseline present + the three intervention worlds by role (B/C/D) with every headline row quoting the baseline and one cell per world, provenance Simulated; a third pins determinism across two keyless calls. Whole-surface `scripts/audit.py` GET sweep now covers it (served-200 + provenance-tag + byte-identical). 3 tests; **472 green** (was 469), `scripts/audit.py` PASS, app boots with **56 routes** (was 55). No frontend/shared files touched.]
- [x] Keyless `GET /evidence/example` — the §26 Explainability causal trace with no request body (SPEC §26/§34) [DONE — `backend/app/routers/evidence.py`: completes the keyless judge-facing surface for the **Explainability / Evidence Drawer** — the SPEC §26 view the whole product is sold on (*"click any output → walk the causal trace all the way down to the underlying evidence"*). Every composed answer endpoint (`/brief/example`, `/run/example`, `/north-star/example`), the §21 quartet (`/compare/example`) and `/backtest/example` already let a judge pull the canonical artifact with **zero request body**, but `POST /evidence` — the endpoint that turns a single metric into the `input-data → transform → model → assumptions → result` ladder — still required a hand-compiled `PolicyDSL` **and** a metric key. `GET /evidence/example` compiles the canonical §28 demo congestion charge and runs the *identical* `run_evidence()` service the POST endpoint uses, on the exact metric SPEC §26 itself motivates the feature with — **peak transit demand** (`transit.peak_into_cbd_transit_trips`, *"why does public transport demand rise?"*) — so the keyless surface lands on the spec's own worked example. **Pure additive routing — no new numeric model, no LLM in any figure** (SPEC §26/§34): a test pins the example **byte-identical** (`json.dumps(sort_keys=True)`) to `POST /evidence` with the same compiled policy + metric, so it can never drift from the real endpoint; a second asserts the full §26 causal ladder (`input-data` first → `result` last, `model` present, result node's Δ = World B − World A, renderable `ascii_trace` non-empty), provenance Simulated; a third pins determinism across two keyless calls. Whole-surface `scripts/audit.py` GET sweep now covers it (served-200 + provenance-tag). 3 tests; **475 green** (was 472), `scripts/audit.py` PASS, app boots with **57 routes** (was 56). No frontend/shared files touched.]
- [x] Truly whole-surface audit GET sweep — close the keyless-example coverage gap (SPEC §34) [DONE — `scripts/audit.py`: the whole-surface §34 audit CLI is meant to hit **every** GET + POST route so a judge can prove no cross-layer contract drift the night before the demo, but its `get_routes` list had silently fallen behind the keyless-example surface — it covered only `/backtest/example` (+ the new `/compare/example`), while the four other no-body judge-facing GETs the engine now serves — `/brief/example`, `/run/example`, `/north-star/example` (the M46–M48 composed answers) and `/robustness/objectives` — were **never swept**, so the M47/M48 progress claims that "the audit GET sweep now covers it" were inaccurate and the heaviest composed payloads (the full §29 pipeline, the §37 15-line answer, the one-page ministerial memo) went unaudited by the standing pre-demo gate. Added all four to `get_routes` so the audit's three GET checks — **served-200** (no cross-layer 500 the night before), **every `provenance` field carries a §34 tag** (recursive walk over these large composed envelopes), and the per-route health list — now genuinely cover the entire live GET surface (10 GETs + the POST sweep). Verified: `scripts/audit.py` still **PASS** with the six §34 guardrail checks green over the expanded surface, and `backend/tests/test_audit_script.py` (5 tests, incl. the ≥35-routes / rubber-stamp-flip assertions) still green. Scripts-track only — no `backend/app/**` behaviour changed, no new numeric model, no LLM (§34); test count unchanged at **472 green**, app boots with **56 routes**. No frontend/shared files touched.]
- [x] Service capability manifest — `GET /capabilities`, the machine-readable "front door" (SPEC §27/§33/§34) [DONE — `backend/app/capabilities/` + `GET /capabilities`: the engine now serves 50+ routes but nothing described the **HTTP surface itself** — `/registry` (§33) catalogues the *models*, `/data-fabric` (§4) the *datasets*, `/openapi.json` is a raw schema dump with no SPEC mapping — so a judge or the UI had no single call that says *what can this engine do, which SPEC section does each endpoint implement, which return numbers vs prose, and which have a no-body example I can hit right now.* `GET /capabilities` is that catalogue: every product route grouped into 12 functional areas, each card carrying its `methods`, `spec_sections`, one-line `summary`, `needs_body`, `output_tag` (provenance class of that route's numbers, or null for prose/mixed/metadata) and its `keyless_example` companion, plus a flat `keyless_examples` list and summary `counts`. Crucially it is **reconciled live against the running app's routes** (`live_route_methods(app)` walks `app.routes`, methods read live not hand-copied) joined to a curated catalogue for the SPEC mapping — any live route with no card surfaces in `undocumented_routes` and any card for a deleted route in `phantom_cards`, **both asserted empty by a standing test**, so the manifest can never silently fall behind the surface (the same can't-drift discipline as the §33 registry reading params live). FastAPI docs infra (`/docs`, `/redoc`, `/openapi.json`, `/docs/oauth2-redirect`) is deliberately excluded as non-product. The manifest is **Observed** about the service (transparency artifact, not a forecast); the per-endpoint tag field is named `output_tag` (not `provenance`) so the whole-surface §34 provenance walk enforces only the real top-level tag, not this descriptive echo. Deterministic (two identical calls byte-identical — guarded); no numeric model, no LLM (§34); root `/` now advertises `capabilities: /capabilities`; added to the `scripts/audit.py` GET sweep (served-200 + provenance-tag). 9 tests; **484 green** (was 475), `scripts/audit.py` PASS, app boots with **58 routes** (was 57). No frontend/shared files touched.]

## Demo tooling (scripts/)
- [x] Judge-runnable killer-demo CLI — `scripts/demo.py` drives the full `POST /run` pipeline **in-process** (FastAPI `TestClient`, no server/ports) and prints the §29 narrative in the terminal: compiled Policy DSL summary, the headline dashboard at the horizon (A→B, Δ, 
## Demo tooling (scripts/)
- [x] Judge-runnable killer-demo CLI — `scripts/demo.py` drives the full `POST /run` pipeline **in-process** (FastAPI `TestClient`, no server/ports) and prints the §29 narrative in the terminal: compiled Policy DSL summary, the headline dashboard at the horizon (A→B, Δ, %, uncertainty band, provenance tag per tile), net public support, the parliament motion + stance tally + synthesis, the auto-proposed amendment with its `changes` and re-simulated checkpoint count, and one sample SIMULATED media headline per horizon. Ends with a live **§34 guardrail audit** that re-checks the composed payload — every headline metric carries a provenance tag, the Δ band is non-decreasing over the horizon (from `delta.series[*].points[*].low/high`), every media headline is labelled SIMULATED, and debate/media prose is tagged Generated — and returns a **non-zero exit code** if any guardrail fails, so it doubles as a pre-demo/CI smoke check. Flags: `--json` (raw `/run` payload), `--text` (custom policy), `--horizon` (months). No new numeric model, no LLM (SPEC §34); pure read of existing endpoints. Guarded by `backend/tests/test_demo_script.py` (5 tests: runs+audit passes, JSON payload valid, guardrails hold at horizons 12/60). **328 green**, app boots with **42 routes**.
- [x] North-Star runner CLI — `scripts/north_star.py` drives `POST /north-star` **in-process** (FastAPI `TestClient`, no server/ports) and prints the **§37 minister's answer** in the terminal: the question, policy id + horizon, then the **fixed 15-line narrative** (each line = the §37 question + its deterministic one-sentence `lead` + provenance tag), a compact **backing-figures** block (the median-outcome dashboard A→B/%, the risk-reducing amendments + what risk each targets, the optimiser best-balanced pick), and a live **§34 guardrail audit** — all 15 sections present + ordered, every section provenance-tagged, every media headline labelled SIMULATED, `llm_touches_numbers=False` from the registry, and the 95% uncertainty fan **non-decreasing + strictly wider** at the far horizon than at T0 — returning a **non-zero exit code** if any guardrail fails (so it doubles as a pre-demo/CI smoke check, like `demo.py`). This renders the SPEC's literal closing experience ("That is URBAN."), complementing `demo.py`'s §29 pipeline view. Flags: `--json` (raw `/north-star` payload), `--text` (custom policy), `--horizon` (months). No new numeric model, no LLM (SPEC §34); pure read of the `/north-star` endpoint. Separate file — `demo.py` untouched (its guards intact). Guarded by `backend/tests/test_north_star_script.py` (5 tests: script present, runs+audit passes with all 15 lines rendered, `--json` is a valid `/north-star` payload with 15 sections, guardrails hold at horizons 12/60). **345 green**, app boots with **43 routes**.
- [x] Whole-surface §34 guardrail audit CLI — `scripts/audit.py` drives the **entire HTTP surface** (compile the demo policy once, then every GET + POST route) **in-process** (FastAPI `TestClient`, no server/ports) and reports a single judge-facing pass/fail **compliance matrix** — where `demo.py` / `north_star.py` audit one composed payload, this proves the four load-bearing §34 claims across *all* layers at once: (1) **every route serves 200** (catches cross-layer contract drift the night before the demo), (2) **nothing untagged** — every `provenance` field at any depth in any response references an allowed tag (Observed/Estimated/Simulated/Generated), (3) **no LLM touches a number** — every §33-registry model asserts `llm_touches_numbers=False` and generated media carries the SIMULATED banner, (4) **reproducible + honest about the future** — each deterministic numeric layer (`/simulate`, `/spatial`, `/microsim`, `/economy`, `/dynamics`, `/sdg`, `/ensemble`, `/diffusion`, `/public`, `/parliament/failure-modes`, `/stress-test`, seeded `/uncertainty`) returns **byte-identical JSON** across two identical calls, and `/simulate`'s uncertainty fan **widens** (non-decreasing + strictly wider at the far horizon than T0) — prose layers excluded from byte-identity by design (Generated). Prints a per-route health list + the six checks, surfacing the specific offender on any failure, and returns a **non-zero exit code** if any guardrail fails (so it's a real pre-demo/CI gate, not a rubber stamp). This composes the four standing pytest guards (integration-smoke, determinism-regression, widening, cross-layer) into one human-runnable command a judge can run without reading pytest. `build_report()` is importable and returns the structured report so tests assert on it directly. Flags: `--json` (machine-readable report), `--text` (custom policy). No new numeric model, no LLM (SPEC §34); pure read of existing endpoints — `demo.py` / `north_star.py` untouched. Guarded by `backend/tests/test_audit_script.py` (5 tests: script present, full-surface audit passes, JSON report complete + all six checks green over ≥35 routes + ≥15 registry models, the pass/fail rollup **flips when any single check is corrupted** so it can't be a rubber stamp, and a pedestrianisation policy also holds). **350 green**, app boots with **43 routes**.
- [x] Robustness runner CLI — `scripts/robustness.py` drives `POST /robustness` **in-process** (FastAPI `TestClient`, no server/ports) so the decision-under-uncertainty pick is runnable in the terminal alongside `demo.py` / `north_star.py` / `audit.py`. Over a small candidate set (baseline no-op + two charge variants) it prints the **payoff matrix** (candidate × state-of-the-world = baseline + the SPEC §20 shocks), the **regret matrix**, and the pick each classic decision criterion makes — **nominal** (headline winner), **maximin** (best worst-case), **minimax-regret / Savage**, **Laplace** (equal-weight mean) and the stress-test **robustness rate** — then the headline insight (does robustness *change* the choice, or is the headline winner also the robust pick). Ends with a live **§34 guardrail audit** re-checking the composed payload: report tagged Simulated, the regret matrix well-formed (≥0, zero for each state's best), every payoff **byte-equal to the deterministic stress-core Δ(B−A)** (so the CLI can never disagree with `/stress-test` or `/simulate`), byte-identical on repeat, and confidence never *narrows* at long horizon (§24) — returning a **non-zero exit code** if any check fails, so it doubles as a pre-demo/CI smoke check. Flags: `--json` (raw `/robustness` payload), `--objective` (headline metric), `--horizon` (months). No new numeric model, no LLM (SPEC §22/§34); pure read of the `/robustness` endpoint — `demo.py` / `north_star.py` / `audit.py` untouched. Guarded by `backend/tests/test_robustness_script.py` (6 tests: script present, runs+audit passes, `--json` is a valid `/robustness` payload, guardrails hold across objectives/horizons). **405 green** (was 399), app boots with **52 routes**.
- [x] Capability-map CLI — `scripts/capabilities.py` drives `GET /capabilities` **in-process** (FastAPI `TestClient`, no server/ports) so the whole engine's HTTP surface is browsable in the terminal alongside `demo.py` / `north_star.py` / `audit.py` / `robustness.py`. Where `audit.py` *proves the §34 guardrails hold* across every route, this *shows a judge what the routes are*: every product endpoint grouped by the 12 functional areas, each printed with its `methods`, SPEC section(s), a one-line summary, whether it needs a body, its provenance class (colour-coded Observed/Estimated/Simulated/Generated, or `—` for prose/mixed), and its no-body `keyless_example`. Header line summarises the counts (routes / areas / GET vs POST / SPEC sections / keyless examples). `--area "<name>"` filters to one area (unknown area → exit 1 with the valid list); `--json` prints the raw `/capabilities` manifest. Ends with a live **consistency audit** re-checking the manifest: it is tagged **Observed**, its curated catalogue **reconciles exactly with the live route surface** (`undocumented_routes` and `phantom_cards` both empty — so a new route without a card is caught here too), every endpoint `output_tag` is a §34 tag or null, the manifest is **byte-identical on repeat**, and **every advertised keyless example is a served GET (200)** — returning a **non-zero exit code** if any check fails, so it doubles as a pre-demo/CI smoke check. No new numeric model, no LLM (SPEC §34); pure read of the `/capabilities` endpoint — `demo.py` / `north_star.py` / `audit.py` / `robustness.py` untouched. Guarded by `backend/tests/test_capabilities_script.py` (5 tests: script present, runs+audit passes with the load-bearing areas rendered, `--json` is a valid manifest with empty undocumented/phantom, area filter selects one area, unknown area fails with the valid list). Scripts/test-track only — no `backend/app/**` change. 5 tests; **489 green** (was 484), app boots with **58 routes**.

## Presentation / export (judge-facing artifact)
- [x] Minister's Brief export (SPEC §27 Core UI / §28.11 Evidence Drawer / §37) [DONE — `backend/app/brief/` + `POST /brief` (+ `GET /brief/example`): the **one-page ministerial memo** SPEC §27/§28.11 asks for as the exportable/printable artifact behind the dashboard — every prior layer returned raw JSON; nothing rendered the answer as a document a minister could read or hand across a table. Delivered as a **pure rendering layer — no new numeric model, no LLM in any figure** (SPEC §34): `build_brief` delegates the *entire* answer to `run_north_star` (the §37 composition, which itself reuses `/simulate`, `/analogues`, `/uncertainty`, `/microsim`, `/parliament/failure-modes`, `/parliament/debate`, `/diffusion`, `/media`, `/optimise`, `/registry` verbatim) and `render.py` lays it out as a single self-contained **Markdown memo**, so the brief **can never disagree** with the tabs behind it — a test pins `brief.answer.median_outcome == /north-star median_outcome` byte-for-byte and asserts every headline label the memo prints comes from that answer. The memo carries: a title + question + snapped horizon, a printed **provenance key** (all four tags), an **executive-summary headline table** (baseline A → policy B → Δ → Δ% → uncertainty band → provenance tag per metric), the **fixed §37 15-line narrative** (order-preserved — a test finds all 15 `**n. …**` markers and asserts they are monotonically ordered), a **winners/losers + regressivity** block, a ranked **failure-mode table** (Estimated overlay: risk/severity/probability/mitigation), the **risk-reducing amendments** + their re-simulated effect, a **SIMULATED media-narratives** section (banner + outlet/headline/sentiment per horizon, SPEC §15/§34), and a **reproducibility footer** (assumption + guardrail counts, seed line, the no-LLM-in-numbers claim — SPEC §32). Presentation switches: `include_answer` (drop the heavy structured `NorthStarAnswer` for a text-only memo) and `include_media` (drop the media section); both guarded. Response also returns the structured `tag_legend`, `word_count`, and (optionally) the full `answer` so the UI can render either the Markdown or the objects. Deterministic (two identical calls → byte-identical Markdown — guarded); mirrors `/north-star`'s NL-compile-or-precompiled input (a test drives the pre-compiled `policy` path and asserts `compiled is None`); `GET /brief/example` renders the canonical §28 demo charge with no body. Pure additive composition — no existing `backend/app/**` layer changed. 7 tests; **388 green** (was 381), app boots with **50 routes** (was 48).]

## Decision support (choose under uncertainty)
- [x] Robustness / regret ranking — decision-under-uncertainty across candidate policies (SPEC §20/§21/§22) [DONE — `backend/app/robustness/` + `POST /robustness` (+ `GET /robustness/objectives`): the stress-test layer (§20) answers *"does **this** policy hold under the named shocks?"*; the optimiser (§22) finds a good policy *under the baseline*. Nothing composed the two into the decision a minister actually faces — *"given several candidate policies and a set of possible futures, **which should I pick** — the headline winner, or the one that's least bad when the world turns out otherwise?"* This layer scores **every candidate × every state of the world** (the transparent baseline + the SPEC §20 shocks), builds the **regret matrix** (per-state best payoff − candidate payoff, ≥0, zero for the state's best), and applies the classic decision criteria: **nominal** (headline winner), **maximin** (best worst-case payoff), **minimax-regret / Savage** (lowest max regret), **Laplace** (best equal-weight mean), and the stress-test **robustness rate** (fraction of shocks where the candidate retains ≥75% of its own no-shock benefit). The headline states plainly whether robustness *changes* the choice — the demo insight that the policy winning the headline is often not the one to pick once you admit the future is uncertain. **Pure composition — no new numeric model, no randomness, no LLM (SPEC §22/§34):** every payoff is the *exact same* deterministic `Δ(B−A)` the stress core (`_run_delta`) already produces, so this layer can never disagree with `/stress-test` or `/simulate` — a test pins a robustness baseline payoff byte-equal to the `/stress-test` baseline benefit. Objective selectable across the four headline metrics (default emissions), horizon-snapped with confidence widening on the long horizon (§24), 422 on <2 candidates, 404 on an unknown objective/shock (echoing valid keys). Report tagged Simulated. Added to the whole-surface `scripts/audit.py` sweep (served-200, provenance-tag, and byte-identical determinism checks now cover it). Pure additive — no existing `backend/app/**` layer changed. 11 tests (incl. unit tests pinning both `_headline` branches — flip vs no-trade-off); **399 green** (was 388), app boots with **52 routes** (was 50).]
